# SuperBizAgent 新手教学指南

> 目标读者：懂一点 Python、没接触过 AI 应用开发的新手。
> 目标：让你**彻底看懂**这个项目——每一行代码在干嘛、用了什么知识、为什么这么写，以及**应该按什么顺序读**。

---

## 目录

- [第 0 章 这个项目是什么](#第-0-章-这个项目是什么)
- [第 1 章 技术栈全景：你需要的最低前置知识](#第-1-章-技术栈全景你需要的最低前置知识)
- [第 2 章 架构总览：这个项目怎么组织](#第-2-章-架构总览这个项目怎么组织)
- [第 3 章 推荐阅读顺序（学习路线）](#第-3-章-推荐阅读顺序学习路线)
- [第 4 章 逐文件精讲（核心章节）](#第-4-章-逐文件精讲核心章节)
  - [4.1 配置与启动](#41-配置与启动)
  - [4.2 数据模型层](#42-数据模型层)
  - [4.3 工具与日志](#43-工具与日志)
  - [4.4 API 路由层](#44-api-路由层)
  - [4.5 向量数据库与 RAG 全链路](#45-向量数据库与-rag-全链路)
  - [4.6 RAG Agent 对话服务](#46-rag-agent-对话服务)
  - [4.7 MCP 客户端](#47-mcp-客户端)
  - [4.8 AIOps 智能运维（Plan-Execute-Replan）](#48-aiops-智能运维plan-execute-replan)
  - [4.9 Skill 业务技能系统](#49-skill-业务技能系统)
  - [4.10 MCP 服务器（模拟数据服务）](#410-mcp-服务器模拟数据服务)
  - [4.11 前端（简述）](#411-前端简述)
- [第 5 章 三大核心机制深挖](#第-5-章-三大核心机制深挖)
- [第 6 章 动手练习](#第-6-章-动手练习)
- [第 7 章 常见问题排查](#第-7-章-常见问题排查)
- [附录：名词速查表](#附录名词速查表)

---

## 第 0 章 这个项目是什么

一句话：**一个"能自己查资料回答问题 + 能自动排查服务器故障"的 AI 助手，跑在一个网页里。**

它有两副面孔：

| 面孔 | 功能 | 核心技术 |
|------|------|----------|
| 智能对话 | 你上传一堆文档，它"读懂"后，能基于文档内容回答你的问题 | **RAG**（检索增强生成） |
| AIOps 诊断 | 服务器告警了，它自动制定排查计划、调用工具查日志/查监控、最后生成一份诊断报告 | **Plan-Execute-Replan** |

底层技术：**FastAPI**（Web 服务）+ **LangChain/LangGraph**（大模型应用框架）+ **阿里云 DashScope 千问**（大模型）+ **Milvus**（向量数据库）+ **MCP**（工具协议）。

用一张图感受它的工作：

```
浏览器 (static/)
   │  HTTP / SSE
   ▼
FastAPI 主服务 (app/main.py)
   │
   ├── /api/chat       ──► RAG Agent（LangGraph 智能体）──► 向量库 Milvus
   │                       └── 工具：retrieve_knowledge / get_current_time
   │
   ├── /api/aiops      ──► Plan-Execute-Replan 工作流 ──► MCP 工具（查日志/查监控）
   │
   ├── /api/upload     ──► 文档解析 → 分割 → 向量化 → 存入 Milvus
   │
   └── /api/health     ──► 健康检查
```

**学习这个项目的最大价值**：它是一个"生产级"的 AI 应用范本。市面上很多教程只教你调一次 API，而这个项目展示了 AI 应用落地时真正要考虑的东西：会话管理、流式输出、向量检索精排、工具调用容错、上下文压缩、技能编排。读懂了它，你就掌握了当前 AI 应用开发的主流套路。

---

## 第 1 章 技术栈全景：你需要的最低前置知识

> 这一章不求全部背下来，**先建立"它们是什么"的认知**，读到具体代码时再回来查。文档后面每讲一个文件，都会标注用了这里的哪些知识。

### 1.1 Python 语言特性

这个项目用了不少"现代 Python"写法，你必须认识它们：

| 语法 | 是什么 | 在本项目哪里出现 |
|------|--------|------------------|
| `async def` / `await` | 异步编程。`async def` 定义的函数叫**协程**，用 `await` 等它。好处：等待 IO（网络请求）时不阻塞其他请求 | 所有 API 接口、agent 调用 |
| `async for` | 异步迭代。配合 `async generator`（用 `yield` 产出数据）做流式输出 | `chat_stream`、`aiops` 的 SSE 流 |
| 类型注解 `a: int`、`-> str` | 标注变量/返回类型，方便 IDE 提示和检查 | 几乎所有函数签名 |
| `str \| None` | Python 3.10+ 的类型写法，等价于 `Optional[str]`，表示"字符串或空" | 大量函数参数 |
| `dataclass` | 用几行代码自动生成一个"装数据的类"（自动带 `__init__` 等） | `Skill`、`IndexingResult`、`SearchResult` |
| `@staticmethod` | 装饰器，把方法变成"类级别的函数"，不需要实例就能调用 | `LLMFactory.create_chat_model` |
| `@property` | 把方法伪装成属性，调用时不写括号 | `config.mcp_servers` |
| 单例模式 | 全局只创建一份实例。本项目到处用 `xxx_service = XxxService()` 模块级单例 | `rag_agent_service`、`config` 等 |
| `from __future__ import annotations` | 让类型注解惰性求值，避免提前 import 问题 | `query_metrics_alerts.py` |
| `with ... as`（上下文管理器） | 自动管理资源（如自动关闭连接） | `httpx.Client(...) as client` |
| `@functools.wraps` | 装饰器编程时保留原函数信息（名字、docstring） | MCP 服务器的 `log_tool_call` |

### 1.2 Python 工程化工具

- **uv**：新一代 Python 包管理器，比 pip 快很多。`uv sync` 按 `pyproject.toml` + `uv.lock` 安装依赖。项目根目录的 `.python-version` 指定 Python 版本。
- **pyproject.toml**：现代 Python 项目的"身份证 + 依赖清单 + 工具配置"一体的文件。本项目用它声明依赖、ruff/isort/black/mypy/pytest 等工具配置。
- **.env**：存放密钥等配置，`pydantic-settings` 负责读取（见 4.1）。
- **pydantic**：Python 数据校验库。定义一个 `class X(BaseModel)`，它自动做类型校验、序列化/反序列化，还能生成 JSON Schema。

### 1.3 FastAPI（Web 框架）

- **路由**：`@router.post("/chat")` 装饰一个函数，这个函数就成为一个 HTTP 接口。
- **Pydantic 模型**：接口的入参/出参都用 BaseModel 定义，FastAPI 自动校验请求体。
- **lifespan**：应用启动/关闭时执行的一段逻辑（这里是连接/断开 Milvus）。
- **中间件（Middleware）**：在请求进入路由前、响应返回前统一处理的钩子（这里是 CORS）。
- **静态文件挂载**：`app.mount("/static", StaticFiles(...))` 把文件夹直接变成可访问的网址。
- **SSE（Server-Sent Events）**：服务端往客户端单向推流的 HTTP 技术，用于流式输出。库是 `sse-starlette`。
- **UploadFile**：FastAPI 处理文件上传的类型。

### 1.4 LangChain 全家桶（重点）

LangChain 是"大模型应用开发框架"，它把 AI 开发拆成标准零件：

| 概念 | 类比 | 说明 |
|------|------|------|
| `ChatModel` | 大脑 | 封装大模型 API（`ChatOpenAI`、`ChatQwen`），负责对话生成 |
| `SystemMessage` / `HumanMessage` / `AIMessage` | 对话记录 | 表示"系统设定 / 用户 / 助手"三种角色消息 |
| `tool` 装饰器 | 工具 | 把一个普通 Python 函数变成大模型可以调用的"工具"，大模型会输出"我要调用这个工具、参数是这些" |
| `ToolNode` | 工具执行器 | LangGraph 里负责真正执行工具调用的节点 |
| `Embeddings` | 编码器 | 把文本变成一串数字（向量），语义相近的文本向量距离近 |
| `Document` | 文档对象 | `page_content`（正文）+ `metadata`（元数据），向量库里存的就是它 |
| `Retriever` | 搜索引擎 | 根据问题从向量库召回相关文档 |
| `ChatPromptTemplate` | 提示词模板 | 带 `{占位符}` 的提示词，运行时填充 |
| `with_structured_output` | 强制格式化 | 让模型按 Pydantic 模型的结构输出（如"输出必须是 `steps` 列表"） |
| `create_agent` | 组装智能体 | 把 模型 + 工具 + 记忆 + 中间件 一键组装成一个"会用工具的智能体" |
| `SummarizationMiddleware` | 记忆压缩 | 上下文太长时自动把历史对话总结成摘要 |
| `MemorySaver` | 记忆存储 | 把对话状态（检查点）存在内存里，靠 `thread_id` 区分不同会话 |
| `bind_tools` | 绑定工具 | 告诉模型"你有这些工具可以用" |

### 1.5 LangGraph（工作流引擎）

LangGraph 把 AI 流程画成一张**图**：

- **State（状态）**：图运行中共享的一个字典，所有节点都能读它、改它。
- **Node（节点）**：图里的一个处理函数，输入 State，输出对 State 的更新。
- **Edge（边）**：节点之间的连线，表示执行顺序。
- **Conditional Edge（条件边）**：根据 State 内容决定下一步走哪条路。
- **checkpointer（检查点）**：保存每个节点的执行结果，用于断点续跑和会话管理。
- **stream_mode**：`"updates"`（每步返回一次更新）、`"messages"`（逐 token 返回消息）。

本项目用了两种 LangGraph 用法，这是最重要的学习点：
1. `aiops_service.py` —— **手写** StateGraph：planner → executor → replanner → (条件) → 结束。
2. `rag_agent_service.py` —— 用 `create_agent` 一行搞定智能体图（内部也是 LangGraph）。

### 1.6 MCP（Model Context Protocol）

MCP 是"给 AI 接工具的行业标准协议"。类比：LLM 是 USB 插孔，MCP 就是 USB-C 接口——任何符合协议的"工具服务器"（日志查询、监控数据…）都能即插即用。

- **MCP Server**：提供工具的服务（本项目 `mcp_servers/` 里有两个，用 FastMCP 库写的）。
- **MCP Client**：主服务里去连接 Server 并获取工具（`app/agent/mcp_client.py`）。
- **transport**：客户端和服务器之间的通信方式。本项目用 `streamable-http`（HTTP），也可能用 `sse`。
- **拦截器（interceptor）**：在工具调用前后"插一手"的钩子，本项目用它做失败重试。

### 1.7 Milvus（向量数据库）

普通数据库按行存数据，向量数据库按"向量"存数据，支持"找最相似"的检索。

- **Collection**：一张"表"。本项目就一张表 `biz`。
- **Field（字段）**：表结构。本项目 4 个字段：`id`（主键）、`vector`（向量）、`content`（文本）、`metadata`（元数据 JSON）。
- **索引**：加速检索的数据结构。本项目 `IVF_FLAT`（聚类索引）+ `L2`（欧氏距离）相似度。
- **召回（recall）**：查出 `k` 条候选结果。本项目先召回 20 条，再用重排模型精选到 3 条。

### 1.8 其他

- **Loguru**：更友好的日志库，支持彩色输出、按天轮转、异步写盘。
- **DashScope**：阿里云百炼的 Python SDK（`dashscope` 库），本项目用它的 `TextReRank` 做重排。
- **pypdf / python-docx**：解析 PDF 和 Word 文件的库。
- **httpx**：Python 发 HTTP 请求的库（本项目用来查 Prometheus 告警）。

---

## 第 2 章 架构总览：这个项目怎么组织

### 2.1 分层架构

项目采用经典的分层架构（越往上越"对外"，越往下越"基础"）：

```
static/                      ← 表现层（浏览器里跑的页面）
   │  调 HTTP / SSE
app/api/                     ← 接口层：接收请求，返回响应（只做"翻译"，不做业务）
   │
app/services/                ← 业务层：真正的业务逻辑（RAG、AIOps、向量库）
   │
app/agent/                   ← 智能体层：LangGraph 节点（planner/executor/replanner）
app/tools/                   ← 工具层：大模型能调用的函数
app/skills/                  ← 技能层：固定业务流程的加载与匹配
   │
app/core/                    ← 基础设施层：LLM 工厂、Milvus 客户端
app/models/                  ← 数据模型层：请求/响应的数据结构（Pydantic）
app/config.py                ← 配置层：所有可调参数
app/utils/logger.py          ← 日志
mcp_servers/                 ← 独立进程：MCP 工具服务器
aiops-docs/                  ← 知识库原始文档（会被向量化后存进 Milvus）
```

### 2.2 依赖方向

记住一条铁律：**上层依赖下层，下层不依赖上层**。

- `api → services → (core / agent / tools) → config`
- `agent → tools / skills / mcp_client`
- 几乎每层都会用到 `config`（配置）和 `logger`（日志）。

看 import 语句就能验证这个方向，例如 `app/api/chat.py` import 了 `app.services.rag_agent_service`，但 `rag_agent_service.py` 绝不可能 import `app.api`。

### 2.3 三张请求流转图（读代码前先看这里）

**图 A：上传文档 → 知识库**
```
POST /api/upload (file.py)
  → 校验文件名/大小
  → 保存到 uploads/
  → vector_index_service.index_single_file()
      → document_parser_service.extract_text()      # 文件 → 纯文本
      → vector_store_manager.delete_by_source()      # 删掉旧版本数据
      → document_splitter_service.split_document()   # 文本 → 分片
      → vector_store_manager.add_documents()          # 分片 → 向量 → 存 Milvus
```

**图 B：对话问答（RAG）**
```
POST /api/chat_stream (chat.py)
  → rag_agent_service.query_stream()
      → agent.astream(stream_mode="messages")   # LangGraph 智能体
          ├─ 大模型决定：要不要调用工具？
          │   ├─ retrieve_knowledge  ──► Milvus 召回20条 ──► 重排 ──► 取3条
          │   ├─ get_current_time
          │   └─ query_prometheus_alerts
          └─ 逐 token 吐出答案
  → SSE 流式返回给前端
```

**图 C：AIOps 故障诊断（Plan-Execute-Replan）**
```
POST /api/aiops (aiops.py)
  → aiops_service.diagnose(session_id)
      → 拼一段固定任务描述 + 注入当前活跃告警名
      → 进入 LangGraph 工作流：
          planner（制定步骤）
            → 有 Skill 命中？→ 直接采用技能固定步骤（跳过 LLM 规划）
            → 否则 → RAG 检索经验 + LLM 生成 4-6 步计划
          executor（执行第一步，调用 MCP 工具查日志/监控）
          replanner（评估：继续 / 重规划 / 直接出报告）
            → continue → 回到 executor
            → replan  → 换计划再回 executor
            → respond → 生成最终诊断报告
      → SSE 流式返回 status/plan/step_complete/report 事件
```

---

## 第 3 章 推荐阅读顺序（学习路线）

> 新手最容易犯的错：一上来就啃最大的文件（`rag_agent_service.py` 428 行、`replanner.py` 318 行）。那是"结果"，不是"入口"。正确顺序是**先看入口、再看依赖、最后看核心逻辑**——因为每个文件 import 了什么，你就该先读什么。

### 阶段 0：跑起来（0.5 小时）

先让项目在本地跑起来，知道"终点长什么样"。按 README 的 Windows 步骤操作，或用 `start-windows.bat`。跑起来后打开 `http://localhost:9900` 点几个按钮，看它回答、看它诊断。

### 阶段 1：配置与入口（读 4.1）

1. `pyproject.toml` —— 看依赖，认识"用到了哪些库"
2. `.env` —— 认识有哪些配置项
3. `app/config.py` —— 配置类，之后几乎所有文件都用它
4. `app/utils/logger.py` —— 日志
5. `app/__init__.py` —— 注意它的 import 副作用
6. `app/main.py` —— 应用怎么启动、路由怎么注册

### 阶段 2：数据模型（读 4.2）

`app/models/` 四个文件。很短，但能让你知道"接口收到/返回什么"。

### 阶段 3：API 层（读 4.4）

`app/api/` 四个文件。看接口怎么调用 service，先别深究 service 内部。

### 阶段 4：RAG 数据链路（读 4.5，重点）

这是"知识库"的底层：

1. `document_parser_service.py` —— 文件变文本
2. `document_splitter_service.py` —— 文本变分片
3. `vector_embedding_service.py` —— 文本变向量
4. `core/milvus_client.py` —— 连数据库
5. `vector_store_manager.py` —— 增删改查向量库
6. `vector_index_service.py` —— 编排"索引一个文件/目录"
7. `vector_search_service.py` —— 检索（备用实现）
8. `vector_rerank_service.py` —— 重排精排
9. `tools/knowledge_tool.py` —— 把检索封装成"大模型可调用的工具"

> 小建议：这一阶段是理解 RAG 的关键，最好对照第 5 章的"RAG 全链路"图来读。

### 阶段 5：对话智能体（读 4.6 + 4.7）

1. `tools/time_tool.py`、`tools/query_metrics_alerts.py` —— 另外两个工具
2. `tools/__init__.py` —— 工具怎么汇总
3. `agent/mcp_client.py` —— MCP 客户端（智能体连接外部工具的桥梁）
4. `services/rag_agent_service.py` —— **本项目最核心文件**，逐行读

### 阶段 6：AIOps（读 4.8）

1. `agent/aiops/state.py` —— 状态定义
2. `agent/aiops/planner.py` —— 计划
3. `agent/aiops/executor.py` —— 执行
4. `agent/aiops/replanner.py` —— 重规划/出报告
5. `services/aiops_service.py` —— 把它们组装成图

### 阶段 7：Skill 系统（读 4.9）

1. `app/skills/loader.py`
2. `app/skills/matcher.py`
3. `app/skills/cpu_high_usage/SKILL.md`（当样例看）
4. 回头看 `planner.py` 里"命中 skill 跳过规划"那段

### 阶段 8：MCP 服务器（读 4.10）

`mcp_servers/cls_server.py` 和 `monitor_server.py`。它们和主服务是**两个独立进程**，各自跑一个 HTTP 服务，主服务通过 MCP 协议连它们。

### 阶段 9（可选）：前端

`static/` 下的 `index.html` / `app.js` / `styles.css`。只需要知道：前端是纯静态单页应用，用 `fetch` 调普通接口、用 `EventSource` 收 SSE 流。不用深究。

---

## 第 4 章 逐文件精讲（核心章节）

> 从这一章开始，我会**逐段讲代码**。格式约定：
> - **职责**：这个文件干嘛的
> - **逐段讲解**：引用关键代码 + 解释"在干嘛/为什么"
> - **知识点**：用到了哪些知识
> - **联系**：和哪些文件协作

---

### 4.1 配置与启动

#### 文件：`pyproject.toml`（项目配置）

**职责**：声明项目元信息、依赖、代码工具配置。

**关键内容**：

```toml
[project]
name = "super-biz-agent-py"
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "sse-starlette>=2.1.0",     # SSE 流式输出的库
    "langchain>=0.1.0",
    "langchain-community>=0.0.20",
    "langchain-core>=0.1.0",
    "langchain-openai>=1.0.0",   # ChatOpenAI（兼容模式调千问）
    "langgraph>=0.0.40",         # 工作流引擎
    "langchain-qwq>=0.3.4",      # ChatQwen：千问原生集成
    "langchain-milvus>=0.3.3",   # Milvus 向量库的 LangChain 封装
    "langchain-mcp-adapters>=0.2.1",  # MCP 客户端适配
    "fastmcp>=2.14.0",           # MCP 服务器框架（mcp_servers/ 用）
    "dashscope>=1.14.0",         # 阿里云百炼 SDK（重排用）
    "pymilvus>=2.3.5",           # Milvus 官方 Python 客户端
    "pydantic>=2.5.0,<3.0.0",
    "pydantic-settings>=2.1.0",  # 从 .env 读配置
    "loguru>=0.7.2",             # 日志
    "python-dotenv>=1.0.0",
    "pypdf>=4.0.0",              # 解析 PDF
    "python-docx>=1.1.0",        # 解析 Word
]
```

**知识点**：
- **uv.lock**：锁文件，锁定每个依赖的精确版本，保证团队环境一致。修改依赖后运行 `uv sync` 更新它。
- `[tool.black]`、`[tool.ruff]`、`[tool.isort]`、`[tool.mypy]`、`[tool.pytest]`：都是代码质量工具的配置段。
- `[project.optional-dependencies].dev`：开发时额外装的依赖（pytest、black 等）。

#### 文件：`.env`（环境变量，敏感文件）

**职责**：存放密钥和可调参数，**不要提交到 git**（`.gitignore` 里会排除它）。

关键项：
```
DASHSCOPE_API_KEY=your-api-key      # 必填：千问的密钥
DASHSCOPE_API_BASE=...              # 默认用新加坡站点，建议配 compatible-mode/v1
DASHSCOPE_MODEL=qwen-max            # 对话模型
MILVUS_HOST=localhost
MILVUS_PORT=19530
RAG_TOP_K=3                          # 重排后返回给大模型的文档数
RAG_RECALL_K=20                      # 重排前召回的候选数
CHUNK_MAX_SIZE=800                   # 分片大小
CHUNK_OVERLAP=100                    # 分片重叠
```

**知识点**：12-factor 配置原则——把配置从代码里拿出来，用环境变量管理。

#### 文件：`app/config.py`（配置）

**职责**：定义全局配置对象 `config`，所有文件都从它读参数。

```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",            # 从 .env 文件读
        env_file_encoding="utf-8",
        case_sensitive=False,       # 环境变量大小写不敏感
        extra="ignore",             # .env 里多出的键忽略，不报错
    )
    # 类属性 = 配置项。名字与 .env 键一致（忽略大小写）
    app_name: str = "SuperBizAgent"
    debug: bool = False
    host: str = "0.0.0.0"
    port: int = 9900
    dashscope_api_key: str = ""
    rag_top_k: int = 3
    ...
    @property
    def mcp_servers(self) -> Dict[str, Dict[str, Any]]:
        """把 MCP 配置整理成统一的字典结构"""
        return {
            "cls": {"transport": ..., "url": ...},
            "monitor": {"transport": ..., "url": ...},
        }

config = Settings()   # 模块级单例
```

**逐段讲解**：
- `class Settings(BaseSettings)`：继承 pydantic-settings 的 BaseSettings。**类里的注解属性就是配置项**，字段名即环境变量名。
- `model_config`：告诉它从 `.env` 读，忽略多余字段。
- `rag_context_window: int = 30720`、`rag_context_trigger_fraction: float = 0.7`：这两项是**上下文自动压缩**的配置（见第 5 章），记住它们。
- `@property def mcp_servers`：把 `mcp_cls_transport`、`mcp_cls_url` 等多个散字段，整理成 MCP 客户端要的嵌套字典。**这是"把配置组织成对外结构"的好习惯**。
- `config = Settings()`：模块底部创建全局单例，其他文件 `from app.config import config` 就能用。

**知识点**：pydantic-settings、`@property`、模块级单例。

**联系**：几乎每个文件都 `from app.config import config`。

#### 文件：`app/__init__.py`（包初始化）

```python
__version__ = "1.0.0"
from app.utils import logger  # noqa: F401
```

**逐段讲解**：Python 里 `import app` 时，会先执行包目录下 `__init__.py`。这一行 `from app.utils import logger` 让日志模块在**任何代码 import app 之前就被初始化**（logger.py 底部直接调用了 `setup_logger()`）。

**这是一个容易踩坑的设计**：因为 `app/utils/logger.py` 又 import 了 `app.config`，而 `app/core/...` 也可能 import config——如果顺序不对会循环 import。这里的 `# noqa: F401` 是告诉 ruff 检查器"这个 import 不是没用的"。

**知识点**：Python 包初始化机制、import 副作用、`# noqa` 注释。

#### 文件：`app/utils/logger.py`（日志）

```python
def setup_logger():
    logger.remove()   # 1. 移除默认处理器
    logger.add(sys.stdout, format="...", level="DEBUG" if config.debug else "INFO", colorize=True, backtrace=True, diagnose=config.debug)   # 2. 控制台
    logger.add("logs/app_{time:YYYY-MM-DD}.log", rotation="00:00", retention="7 days", compression="zip", encoding="utf-8", enqueue=True, backtrace=True, diagnose=True, level="INFO", format="...")   # 3. 文件

setup_logger()
```

**逐段讲解**：
- Loguru 的工作方式：`logger.add(目标, 参数...)` 就是"往哪里输出、怎么输出"。
- `logger.remove()`：清掉默认输出，避免重复。
- 控制台输出带颜色和完整格式；文件输出到 `logs/app_日期.log`。
- `rotation="00:00"`：每天零点切一个新文件。`retention="7 days"`：只留 7 天。`compression="zip"`：切下来的旧文件压成 zip。`enqueue=True`：异步写盘，不让日志拖慢主流程。`encoding="utf-8"`：避免中文乱码。
- 文件里 `diagnose=True` 但控制台 `diagnose=config.debug`：生产环境不开变量值泄露，防止把敏感信息打进日志。

**知识点**：Loguru 使用、日志轮转、异步日志。

#### 文件：`app/main.py`（FastAPI 入口）

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动时
    milvus_manager.connect()   # 连接 Milvus
    yield
    # 关闭时
    milvus_manager.close()     # 断开 Milvus

app = FastAPI(title=config.app_name, version=config.app_version, lifespan=lifespan)

app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"])

app.include_router(health.router, tags=["健康检查"])
app.include_router(chat.router, prefix="/api", tags=["对话"])
app.include_router(file.router, prefix="/api", tags=["文件管理"])
app.include_router(aiops.router, prefix="/api", tags=["AIOps智能运维"])

app.mount("/static", StaticFiles(directory=static_dir), name="static")
```

**逐段讲解**：
- `lifespan`：FastAPI 的生命周期钩子。`yield` 之前是启动逻辑（连 Milvus），之后是关闭逻辑（断开）。类比"开机自检"和"关机清理"。
- `CORSMiddleware`：允许跨域。`allow_origins=["*"]` 表示任何前端都能访问（生产环境应该限制域名，代码注释也提醒了）。前端和后端不同端口时会用到。
- `include_router(...)`：把每个模块里定义的 `router`（APIRouter）注册进来。`prefix="/api"` 表示这些路由的完整路径是 `/api/chat`、`/api/upload` 等。
- `app.mount("/static", ...)`：把 `static/` 文件夹暴露成 `/static/...` 网址。`app.get("/")` 则直接返回 `index.html`，所以打开 `http://localhost:9900` 就是前端页面。
- 底部 `if __name__ == "__main__":`：`python app/main.py` 直接启动 uvicorn 的写法（不过实际都用 `uvicorn app.main:app`）。

**知识点**：FastAPI lifespan、中间件、路由注册、静态文件。

**联系**：`app/api/*.py` 的 router 都在这里被挂载。

---

### 4.2 数据模型层

> 这层所有文件都极其简单，就是"定义数据结构"，但它们是接口的契约。**记住一句**：FastAPI 会把请求体 JSON 自动转换成这些 Pydantic 模型，也会把模型自动转成响应 JSON。

#### 文件：`app/models/request.py`

```python
class ChatRequest(BaseModel):
    id: str = Field(..., description="会话 ID", alias="Id")
    question: str = Field(..., description="用户问题", alias="Question")
    class Config:
        populate_by_name = True
        json_schema_extra = {"example": {"Id": "session-123", "Question": "什么是向量数据库？"}}
```

**逐段讲解**：
- `alias="Id"`：JSON 里字段名是 `Id`（大写），Python 里用 `id`。这个别名让前端可以传 `{"Id": ..., "Question": ...}`（看 README 的 curl 示例确实这样传）。
- `populate_by_name = True`：允许同时用 `Id` 或 `id` 两种名字传参。
- `json_schema_extra`：给 Swagger 文档（`/docs`）配一个示例请求体，方便调试。
- `ClearRequest`：清空会话的请求，`sessionId` 别名。

**知识点**：Pydantic `Field`、别名机制。

#### 文件：`app/models/response.py` / `app/models/aiops.py` / `app/models/document.py`

- `response.py`：定义 `ChatResponse`、`SessionInfoResponse`、`ApiResponse`、`HealthResponse`。注意 `ApiResponse` 的 `data: Optional[Any]`——通用响应壳子（`status`/`message`/`data` 三段式，很多公司接口都这么封装）。
- `aiops.py`：`AIOpsRequest`（诊断请求，只有一个可选 `session_id`）、`AlertInfo`、`DiagnosisResponse`。
- `document.py`：`DocumentChunk`，定义"一个分片"的样子。**这个模型现在其实没被使用**（分割器直接生成 LangChain 的 `Document`），可以看作遗留定义。

**知识点**：Pydantic 定义响应结构、`Optional` 字段。

---

### 4.3 工具与日志

#### 文件：`app/core/llm_factory.py`（LLM 工厂）

```python
class LLMFactory:
    DASHSCOPE_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"

    @staticmethod
    def create_chat_model(model=None, temperature=0.7, streaming=True, base_url=None, api_key=None) -> ChatOpenAI:
        model = model or config.dashscope_model
        base_url = base_url or LLMFactory.DASHSCOPE_BASE_URL
        api_key = api_key or config.dashscope_api_key
        llm = ChatOpenAI(model=model, temperature=temperature, streaming=streaming,
                         base_url=base_url, api_key=api_key, extra_body={"stream": streaming})
        return llm

llm_factory = LLMFactory()
```

**逐段讲解**：
- 用 LangChain 的 `ChatOpenAI` 走**OpenAI 兼容模式**访问千问——因为千问提供了 OpenAI 兼容的 API 端点。好处：换任何支持 OpenAI 协议的服务商（OpenAI、Azure、DeepSeek…）只需改 `base_url` 和 `api_key`，代码不用动。
- `extra_body["stream"] = streaming`：DashScope 需要显式告诉它开流式。
- 注意：这个文件定义了工厂，但**对话和诊断实际用的是 `langchain_qwq` 的 `ChatQwen`**（见 4.6/4.8），工厂更像一个备用/示范工具。这正是"项目在演进"的痕迹，不用困惑。

**知识点**：工厂模式、OpenAI 兼容协议、`@staticmethod`。

#### 文件：`app/tools/__init__.py`（工具注册表）

```python
from app.tools.knowledge_tool import retrieve_knowledge
from app.tools.query_metrics_alerts import query_prometheus_alerts
from app.tools.time_tool import get_current_time

DEFAULT_LOCAL_AGENT_TOOLS = (
    retrieve_knowledge,
    get_current_time,
    query_prometheus_alerts,
)
```

**逐段讲解**：把三个本地工具汇总成一个元组 `DEFAULT_LOCAL_AGENT_TOOLS`，RAG Agent 和 AIOps 都用这一个来源，避免两处各维护一份。`__all__` 声明对外可导入的名字。

**知识点**：包级常量、集中管理。

---

### 4.4 API 路由层

> API 层是"翻译官"：把 HTTP 请求翻译成 service 调用，把结果翻译回 JSON/SSE。**API 层不做业务**，业务都在 service。

#### 文件：`app/api/chat.py`（对话接口）

接口：`POST /api/chat`（一次性）、`POST /api/chat_stream`（SSE 流式）、`POST /api/chat/clear`、`GET /api/chat/session/{session_id}`。

**`/api/chat`（非流式）**：
```python
answer = await rag_agent_service.query(request.question, session_id=request.id)
return {"code": 200, "message": "success", "data": {"success": True, "answer": answer, "errorMessage": None}}
```
- `await`：因为 `rag_agent_service.query` 是 async 的（内部要调大模型 API）。
- 返回一个统一的 `{code, message, data}` 外壳——这是前后端约定好的格式。
- 用 try/except 包住，出错时也返回统一格式（`code: 500`），而不是让服务崩掉。**这是"接口层永远返回可解析的 JSON"的健壮性设计**。

**`/api/chat_stream`（流式，重点）**：
```python
async def event_generator():
    async for chunk in rag_agent_service.query_stream(request.question, session_id=request.id):
        chunk_type = chunk.get("type", "unknown")
        chunk_data = chunk.get("data", None)
        if chunk_type == "tool_call":
            yield {"event": "message", "data": json.dumps({"type": "tool_call", "data": chunk_data}, ensure_ascii=False)}
        elif chunk_type == "content":
            yield {"event": "message", "data": json.dumps({"type": "content", "data": chunk_data}, ensure_ascii=False)}
        ...
return EventSourceResponse(event_generator())
```

**逐段讲解**：
- `event_generator` 是一个**异步生成器**（有 `yield`），每次 `yield` 就向客户端吐一段 SSE 数据。
- `rag_agent_service.query_stream` 返回的每个 `chunk` 是个字典，用 `type` 区分：`content`（答案片段）、`tool_call`（工具调用状态）、`search_results`（检索结果）、`complete`（结束）、`error`（错误）。
- SSE 要求 data 必须是**字符串**，所以用 `json.dumps(..., ensure_ascii=False)` 把事件序列化成 JSON 字符串（`ensure_ascii=False` 保证中文不被转成 `\uXXXX`）。
- `EventSourceResponse` 来自 sse-starlette，把生成器包装成 SSE 响应。
- 第 160 行 `format_exception_chain(e)`：展开嵌套异常打印出"异常链"，方便定位（这个函数在 mcp_client.py 里，见 4.7）。

**`/api/chat/clear` 和 `/api/chat/session/{id}`**：
- 调 `rag_agent_service.clear_session()` 和 `get_session_history()`，操作的是 LangGraph 的 MemorySaver 检查点（见 4.6）。

**知识点**：async generator、SSE 协议、`json.dumps` 序列化、统一响应格式。

**联系**：`app/services/rag_agent_service.py`（下章主角之一）。

#### 文件：`app/api/aiops.py`（AIOps 接口）

接口：`POST /api/aiops`。**流式返回 6 种事件**（文件头的 docstring 写得很清楚）：

| type | 含义 |
|------|------|
| `status` | 过程状态（如"正在获取告警"） |
| `plan` | 计划制定完成，带 `plan` 步骤列表 |
| `step_complete` | 某步执行完成，带 `current_step` / `remaining_steps` |
| `report` | 最终报告（Markdown 文本） |
| `complete` | 全部完成，带 `diagnosis` 对象 |
| `error` | 出错 |

```python
async for event in aiops_service.diagnose(session_id=session_id):
    yield {"event": "message", "data": json.dumps(event, ensure_ascii=False)}
    if event.get("type") in ["complete", "error"]:
        break   # 收到结束事件就断开流
```

**逐段讲解**：和 chat_stream 几乎一样，只是事件内容来自 `aiops_service.diagnose`。`break` 很重要：告诉浏览器"流结束了"，前端才能关闭连接、停止转圈。

**知识点**：SSE 事件设计、流式接口。

#### 文件：`app/api/file.py`（上传接口）

接口：`POST /api/upload`、`POST /api/index_directory`。

```python
SUPPORTED_EXTENSION_NAMES = ", ".join(ext.lstrip(".") for ext in sorted(SUPPORTED_EXTENSIONS))
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB

@router.post("/upload")
async def upload_file(file: UploadFile = File(...)):
    safe_filename = _sanitize_filename(file.filename)          # 1. 规范化文件名
    file_extension = _get_file_extension(safe_filename)
    if f".{file_extension}" not in SUPPORTED_EXTENSIONS:       # 2. 白名单校验扩展名
        if file_extension == "doc":
            raise HTTPException(400, "不支持老版 .doc，请另存为 .docx")
        raise HTTPException(400, f"仅支持: {SUPPORTED_EXTENSION_NAMES}")
    UPLOAD_DIR.mkdir(parents=True, exist_ok=True)
    content = await file.read()
    if len(content) > MAX_FILE_SIZE:
        raise HTTPException(400, "文件过大")
    file_path.write_bytes(content)                              # 3. 保存
    try:
        vector_index_service.index_single_file(str(file_path))  # 4. 自动建索引
    except Exception as e:
        logger.error(...)   # 索引失败不阻塞上传
```

**逐段讲解**：
- **安全第一**：`_sanitize_filename` 把文件名里的 `\ / : * ? " < > |` 和空格全部替换成下划线，防止路径穿越攻击（比如文件名是 `../../etc/passwd`）。
- `SUPPORTED_EXTENSIONS` 从 `document_parser_service` import——**"支持哪些格式"只有一个事实来源**，上传白名单、索引遍历都从它取，改一处即可（代码注释专门强调了这点，好习惯）。
- `.doc` 单独提示：因为 `python-docx` 只支持 `.docx`。
- 上传成功**后**自动索引；索引失败只是记日志，文件本身保留。这是"核心功能不因附加功能失败而失败"的思想。
- `_get_file_extension`：用 `rsplit(".", 1)` 切出最后一个点后面的部分（处理 `a.tar.gz` 这类多后缀）。

**知识点**：文件上传、路径安全、`UploadFile`、白名单校验、异常分层（`HTTPException` 直接 raise，业务异常包成 500）。

#### 文件：`app/api/health.py`（健康检查）

```python
health_data["milvus"] = {"status": ..., "message": ...}
if health_data["milvus"]["status"] != "connected":
    overall_status = "unhealthy"; status_code = 503   # 数据库挂了 → 服务降级为不可用
return JSONResponse(status_code=status_code, content={"code":..., "message":..., "data": health_data})
```

**逐段讲解**：检查服务本身 + Milvus 连接状态。Milvus 连不上就把 HTTP 状态码改成 503，并报告"数据库不可用"。这样部署平台（K8s 探活等）能据此判断要不要重启服务。

**知识点**：健康检查约定（200/503）、依赖健康传播。

---

### 4.5 向量数据库与 RAG 全链路

> 这是"知识库"部分，RAG 的地基。阅读时对照第 5 章的 RAG 全链路图。

#### 文件：`app/services/document_parser_service.py`（文件 → 纯文本）

```python
SUPPORTED_EXTENSIONS = frozenset({".md", ".txt", ".pdf", ".docx"})

class DocumentParserService:
    _dispatcher: Dict[str, str] = {
        ".md": "_extract_plain", ".txt": "_extract_plain",
        ".pdf": "_extract_pdf",   ".docx": "_extract_docx",
    }
    def extract_text(self, file_path: str) -> str:
        ext = path.suffix.lower()
        handler_name = self._dispatcher.get(ext)
        if handler_name is None:
            raise ValueError(f"不支持的文件类型: {ext}")
        handler = getattr(self, handler_name)   # 根据扩展名动态找到处理方法
        return handler(resolved)
```

**逐段讲解**：
- **分派表（dispatcher）**：一个字典，键是扩展名，值是方法名。`getattr(self, handler_name)` 动态取方法。**加新格式只需两步**：写一个 `_extract_xxx` 方法 + 在表里加一行。这是"策略模式"的轻量实现。
- `_extract_plain`：`.md`/`.txt` 直接 `read_text(encoding="utf-8")`。
- `_extract_pdf`：用 pypdf 逐页 `page.extract_text()`，页间用换行连接。遇到加密 PDF 先试空密码解密；**扫描件（图片型）PDF 没有文本层，会返回空串**——这是已知限制，注释写得很诚实。
- `_extract_docx`：用 python-docx 遍历 `doc.paragraphs` 取每段 `p.text`。Word 本质是 zip 包里的 XML，这个库负责解析。表格暂不提取。

**知识点**：策略模式（分派表）、第三方解析库、职责单一（"只负责变成文本"）。

#### 文件：`app/services/document_splitter_service.py`（文本 → 分片）

```python
self.markdown_splitter = MarkdownHeaderTextSplitter(
    headers_to_split_on=[("#", "h1"), ("##", "h2")], strip_headers=False)
self.text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=self.chunk_size * 2, chunk_overlap=self.chunk_overlap)
```

**逐段讲解**：
- **为什么要切分？** 大模型有输入长度限制，且检索时"整篇文档"太粗糙。把文档切成 800 字左右的小块，检索才能命中"相关的那段"。
- `MarkdownHeaderTextSplitter`：按 Markdown 标题（`#`/`##`）切分，让每个分片自带章节信息。只按一级、二级标题切，避免过度碎片化。
- `RecursiveCharacterTextSplitter`：递归字符分割器，优先在段落、句子边界切，尽量保持语义完整。`chunk_size * 2` 是为了"先按标题粗切，再按大小补切"的两阶段策略。
- `split_markdown` 的三阶段：按标题切 → 按大小切 → **合并小分片**（`_merge_small_chunks`，小于 300 字且合并后不超限的就并进去，防止出现"一个标题下只有一句话"的无意义分片）。
- 每个分片打上元数据：`_source`（文件路径）、`_extension`、`_file_name`。后面删除旧数据、显示来源都要用它们。
- `split_document` 按扩展名路由：`.md` 走 markdown 流程，其余走普通文本流程。

**知识点**：文本切分策略、两阶段切分、元数据附加、LangChain `Document`。

#### 文件：`app/services/vector_embedding_service.py`（文本 → 向量）

```python
class DashScopeEmbeddings(Embeddings):
    def __init__(self, api_key, model="text-embedding-v4", dimensions=1024):
        self.client = OpenAI(api_key=api_key, base_url="https://dashscope.aliyuncs.com/compatible-mode/v1")

    def embed_documents(self, texts: List[str]) -> List[List[float]]:
        response = self.client.embeddings.create(model=self.model, input=texts, dimensions=self.dimensions, encoding_format="float")
        return [item.embedding for item in response.data]

    def embed_query(self, text: str) -> List[float]:
        response = self.client.embeddings.create(model=self.model, input=text, dimensions=self.dimensions, encoding_format="float")
        return response.data[0].embedding
```

**逐段讲解**：
- 继承 LangChain 的 `Embeddings` 接口（必须实现 `embed_documents` 和 `embed_query` 两个方法），这样 LangChain 全家桶（Milvus、retriever）都能自动用它。
- 底层用 OpenAI SDK 调千问的 embedding 接口，**文本 → 1024 维浮点数数组**。
- `embed_documents` 支持传列表（批量），省 API 调用次数。
- 构造函数里有 `_mask_api_key` 掩码打印，防止密钥全量出现在日志。
- 如果没配 API Key（等于 `"your-api-key-here"`），直接 `raise ValueError` 快速失败，避免带病运行。

**知识点**：Embeddings 接口、嵌入模型、批量 API、日志脱敏。

#### 文件：`app/core/milvus_client.py`（Milvus 连接管理）

> 这个文件信息量大，包含了几个"只有踩过坑才会写的代码"。

```python
class MilvusClientManager:
    COLLECTION_NAME = "biz"
    VECTOR_DIM = 1024
    def connect(self):
        _patch_pymilvus_milvus_client_orm_alias()   # (见下)
        connections.connect(alias="default", host=..., port=...)
        self._client = MilvusClient(uri=f"http://{host}:{port}")
        if not self._collection_exists():
            self._create_collection()
        else:
            self._collection = Collection(self.COLLECTION_NAME)
            # 检查向量维度是否匹配，不匹配就删了重建（防止脏数据）
            ...
        self._load_collection()
```

**逐段讲解**：
- `connections.connect(alias="default", ...)`：pymilvus 的 ORM 连接。**任何 `Collection` 操作前必须先建立连接**，否则报 `should create connection first`。
- `MilvusClient(uri=...)`：pymilvus 另一个"更上层"的客户端。两种 API 并存容易混乱，所以代码用 `_patch_pymilvus_milvus_client_orm_alias` 这个**猴子补丁**把内部别名统一成 `default`，让两种 API 用同一条连接。这是很 hack 的兼容性修复，注释解释得很清楚。
- `_create_collection()`：建表。4 个字段：
  - `id`：VARCHAR 主键（用 UUID 字符串）
  - `vector`：FLOAT_VECTOR，1024 维
  - `content`：VARCHAR，存分片文本
  - `metadata`：JSON，存元数据
- `_create_index()`：给 vector 字段建 `IVF_FLAT`（倒排文件聚类索引）+ `L2`（欧氏距离）索引，`nlist=128`（聚类中心数）。
- **维度自检**：连接时检查已有 collection 的向量维度是否还是 1024，不一致就 `drop_collection` 重建——防止模型换了维度导致查询崩溃。
- `_load_collection()`：把 collection 加载进内存才能查询。`utility.load_state` 检查是否已加载，兼容不同 pymilvus 版本（用 AttributeError 兜底）。
- `health_check()`：列一下连接就知道通不通。
- `close()`：release + disconnect，逐个 try/except 收集错误（保证一个失败不拖累另一个）。
- 实现了 `__enter__` / `__exit__`（上下文管理器），支持 `with milvus_manager:` 用法。

**知识点**：向量数据库 schema、索引类型、幂等连接、猴子补丁、上下文管理器、防御式编程。

#### 文件：`app/services/vector_store_manager.py`（向量库操作封装）

```python
def _initialize_vector_store(self):
    _ = milvus_manager.connect()   # 必须提前连接！
    self.vector_store = Milvus(
        embedding_function=vector_embedding_service,
        collection_name="biz", connection_args=connection_args,
        auto_id=False, drop_old=False,
        text_field="content", vector_field="vector",
        primary_field="id", metadata_field="metadata",
    )
```

**逐段讲解**：
- 这里用的是 `langchain_milvus` 的 `Milvus` **高层封装**（自动做 embedding、批量插入），字段映射到上面建的 collection。
- `auto_id=False`：自己提供 id（UUID）。
- `add_documents(documents, ids=ids)`：自动批量向量化入库，返回生成的 id。
- `delete_by_source(file_path)`：**按 `_source` 元数据删除旧数据**。查询表达式 `metadata["_source"] == "..."` 是 Milvus 的 JSON 字段过滤语法。作用：同一个文件重新上传时，先删旧的再插新的，实现"覆盖更新"。
- `similarity_search(query, k)`：普通相似度检索（备用方法）。
- **注意模块级单例 `vector_store_manager = VectorStoreManager()`**：也就是说，**import 这个模块时就已经连上 Milvus 了**，早于 FastAPI lifespan 里的 `milvus_manager.connect()`。注释专门解释了为什么要提前连。这意味着 Milvus 必须可用，否则整个 app 导入就崩——设计上的 trade-off。

**知识点**：LangChain Milvus 封装、JSON 过滤表达式、覆盖更新策略、模块导入副作用。

#### 文件：`app/services/vector_index_service.py`（索引编排）

```python
class VectorIndexService:
    def index_single_file(self, file_path):
        content = document_parser_service.extract_text(str(path))   # 1. 文件→文本
        vector_store_manager.delete_by_source(normalized_path)       # 2. 删旧数据
        documents = document_splitter_service.split_document(content, normalized_path)  # 3. 文本→分片
        if documents:
            vector_store_manager.add_documents(documents)            # 4. 分片→向量库
```

**逐段讲解**：
- 编排者角色：把 parser、splitter、store 串起来。**你自己也会想：为什么分成 4 个 service？** 因为每一步都值得独立测试和替换（换解析器、换切分策略、换向量库都不影响其他环节）。
- `index_directory`：遍历目录下所有支持扩展名的文件，逐个索引，统计成功/失败并产出 `IndexingResult`（一个纯数据类，带 `to_dict()` 序列化）。
- `IndexingResult` 的 `get_duration_ms`：记录起止时间算耗时——性能观测的朴素实现。

**知识点**：门面/编排模式、`dataclass` 风格的结果对象、容错（单文件失败不中断整批）。

#### 文件：`app/services/vector_search_service.py`（检索，备用实现）

```python
def search_similar_documents(self, query, top_k=3):
    query_vector = vector_embedding_service.embed_query(query)   # 1. 问题→向量
    collection = milvus_manager.get_collection()
    search_params = {"metric_type": "L2", "params": {"nprobe": 10}}
    results = collection.search(data=[query_vector], anns_field="vector",
                                param=search_params, limit=top_k,
                                output_fields=["id", "content", "metadata"])
    # 解析：hit.entity.get(...) 取字段，hit.distance 是相似度（L2 越小越相似）
```

**逐段讲解**：
- 这条路径是"手动版检索"（不走 LangChain 封装）：问题向量化 → `collection.search` → 解析。`nprobe` 是 IVF 索引的探测簇数。
- **和 knowledge_tool 用的不是同一条路径**（knowledge_tool 用的是 `vector_store.as_retriever`）。这个 service 目前像是更早的实现，被保留着。看懂它反而有助于理解向量检索本质。

**知识点**：向量检索原理（query 向量化 + 相似度计算）、pymilvus search API。

#### 文件：`app/services/vector_rerank_service.py`（重排精排）

> 重排是 RAG 的关键优化：向量检索召回 20 条粗选，重排模型精排后只留 3 条给大模型。**召回是"查得全"，重排是"排得准"。**

```python
class RerankError(Exception): ...   # 专用异常

def rerank(self, query, documents, top_n):
    if not documents: return []
    if len(documents) == 1: return [(documents[0], 1.0)]   # 单条无需重排（优化，非降级）
    resp = TextReRank.call(model=self.model, query=query, documents=texts,
                           top_n=min(top_n, len(documents)), api_key=self.api_key)
    if resp.status_code != 200:    # SDK 不抛异常，必须手动校验
        raise RerankError(...)     # ← "不降级"原则的关键
    ranked = [(documents[r.index], r.relevance_score) for r in resp.output.results]
```

**逐段讲解**：
- 用 `dashscope.TextReRank`（百炼 `gte-rerank-v2` 模型）对 query 和候选文档打分排序。
- **"不降级"设计**（文件头 docstring 专门强调）：重排失败必须抛 `RerankError`，**绝不静默回退**到"只靠向量检索"的结果。因为没精排的结果质量不可控。这个设计哲学值得记住——**失败要大声，不要带病运行**。
- `resp.status_code != 200` 的手动校验：dashscope SDK 在 HTTP 失败时不抛异常，只返回带错误码的响应。不检查就会拿到空结果当成功。注释把这称为"不降级的唯一强制点"。
- 结果用 `result.index` 映射回原始文档（重排返回的是索引+分数，不是文档本身）。
- 防御：超过 `MAX_RERANK_DOCS=100` 截断（正常 `RAG_RECALL_K=20` 不会触发）。

**知识点**：Rerank 模型、语义精排、"失败即显式错误"设计、防御式编程。

#### 文件：`app/tools/knowledge_tool.py`（知识检索工具）

```python
@tool(response_format="content_and_artifact")
def retrieve_knowledge(query: str) -> Tuple[str, List[Document]]:
    """从知识库中检索相关信息来回答问题..."""
    vector_store = vector_store_manager.get_vector_store()
    retriever = vector_store.as_retriever(search_kwargs={"k": config.rag_recall_k})  # 召回 20 条
    docs = retriever.invoke(query)
    if not docs:
        return "没有找到相关信息。", []
    ranked = vector_rerank_service.rerank(query, docs, config.rag_top_k)   # 重排后取 3 条
    docs = [doc for doc, _ in ranked]
    context = format_docs(docs)
    return context, docs
```

**逐段讲解**：
- `@tool(response_format="content_and_artifact")`：把函数变成大模型可调用的工具。`content_and_artifact` 表示返回两个东西：**给大模型看的文本**（content）和**给框架用的原始对象**（artifact，这里是 Document 列表）。
- **这就是 RAG 的完整管道**：召回（20 条）→ 重排（精排）→ 取 top_k（3 条）→ 格式化成上下文。
- `format_docs`：把每个文档包装成"【参考资料 i】标题/来源/内容"的格式——大模型看到"参考资料"这种格式会当成检索证据来引用，回答更有据可依。
- `RerankError` 被单独 catch 后重新 `raise`（不降级），其他异常则返回错误字符串给模型。

**知识点**：`@tool` 装饰器、`content_and_artifact` 返回格式、RAG 检索管道、给模型的提示格式化。

---

### 4.6 RAG Agent 对话服务

#### 文件：`app/services/rag_agent_service.py`（本项目最核心文件）

**职责**：用 LangGraph 组装一个"会用工具的智能体"，提供流式/非流式对话、会话历史读写。

**① 状态定义**
```python
class AgentState(TypedDict):
    messages: Annotated[Sequence[BaseMessage], add_messages]
```
- `TypedDict`：一个"有类型注解的字典"，定义图的状态结构。`Annotated[..., add_messages]` 告诉 LangGraph：多个节点往 `messages` 里写时，用 `add_messages`（追加）而不是覆盖。

**② 模型与上下文压缩中间件**
```python
self.model = ChatQwen(model=self.model_name, api_key=config.dashscope_api_key,
                      temperature=0.7, streaming=streaming,
                      profile={"max_input_tokens": config.rag_context_window})

self.summarization = SummarizationMiddleware(
    self.model,
    trigger=("fraction", config.rag_context_trigger_fraction),   # 上下文占用 70% 触发
    keep=("messages", config.rag_context_keep_messages),          # 保留最近 20 条
    token_counter=self._count_tokens,
)
```
- `ChatQwen` 来自 `langchain_qwq`，是千问的原生 LangChain 集成（`streaming` 更靠谱）。
- `SummarizationMiddleware` 是**上下文自动压缩**：对话变长后，把早期消息总结成一段摘要替换掉，腾出空间。`trigger=("fraction", 0.7)` 表示"上下文占用达到模型的 70% 就压缩"。
- `_count_tokens` 是**精确 token 计数**：代码注释解释了为什么不能省——`SummarizationMiddleware` 默认按"字符数/4"估算 token，对中文严重低估 4 倍，导致 70% 阈值形同虚设。这里用 `self.model.get_num_tokens(text)`（底层走真实 tokenizer）。这是"中文应用必须处理"的典型坑。

**③ 工具与检查点**
```python
self.tools = list(DEFAULT_LOCAL_AGENT_TOOLS)   # 知识检索 + 时间 + Prometheus 告警
self.mcp_tools: list = []                      # MCP 工具（延迟加载）
self.checkpointer = MemorySaver()              # 会话记忆（内存版）
self.agent = None
```

**④ 异步初始化（加载 MCP 工具）**
```python
async def _initialize_agent(self):
    if self._agent_initialized: return
    for name, server in config.mcp_servers.items():
        hint = suggest_mcp_transport(...)      # 检查 transport 配置是否匹配 URL
        if hint: logger.warning(hint)
    mcp_client = await get_mcp_client_with_retry()
    mcp_tools, mcp_err = await load_mcp_tools_safe(mcp_client)   # 失败不抛异常
    if mcp_err:
        self.mcp_tools = []                    # MCP 挂了 → 只用本地工具继续
    else:
        self.mcp_tools = mcp_tools
    all_tools = self.tools + self.mcp_tools
    self.agent = create_agent(
        self.model, tools=all_tools,
        checkpointer=self.checkpointer,
        middleware=[self.summarization],
    )
    self._agent_initialized = True
```
- 懒加载：第一次真正对话时才初始化（因为连接 MCP 可能失败，推迟到调用时才做）。
- **优雅降级**：MCP 工具加载失败只警告，不崩，用本地工具继续。
- `create_agent` 是 LangChain 新版的一行式智能体构造：把 模型 + 工具 + 检查点 + 中间件 打包。

**⑤ 系统提示词**
```python
return dedent("""
    你是一个专业的AI助手，能够使用多种工具来帮助用户解决问题。
    工作原则: ... 回答要求: ...
""").strip()
```
- `dedent`：去掉多行字符串前面的公共缩进，保持提示词干净。注意提示词里**不列举具体工具**——注释解释：LangChain 会自动把工具 schema 传给模型。

**⑥ 非流式查询 `query`**
```python
messages = [SystemMessage(content=self.system_prompt), HumanMessage(content=question)]
config_dict = {"configurable": {"thread_id": session_id}}   # thread_id 区分会话
result = await self.agent.ainvoke(input=agent_input, config=config_dict)
last_message = result.get("messages", [])[-1]
answer = last_message.content
```
- `thread_id`：LangGraph 靠它识别"这是哪个会话"，同一会话的消息自动累积（通过 MemorySaver 检查点）。
- `ainvoke`：一次调用，等全部结果。取最后一跳消息的内容作为答案。

**⑦ 流式查询 `query_stream`（重点）**
```python
async for token, metadata in self.agent.astream(input=..., config=...,
                                                stream_mode="messages"):
    node_name = metadata.get('langgraph_node', 'unknown')
    message_type = type(token).__name__
    if message_type in ("AIMessage", "AIMessageChunk"):
        content_blocks = getattr(token, 'content_blocks', None)
        if content_blocks and isinstance(content_blocks, list):
            for block in content_blocks:
                if isinstance(block, dict) and block.get('type') == 'text':
                    text_content = block.get('text', '')
                    if text_content:
                        yield {"type": "content", "data": text_content, "node": node_name}
yield {"type": "complete"}
```
- `stream_mode="messages"`：**逐消息/逐 token** 流式。`token` 是消息片段，`metadata` 带节点信息。
- 用 `type(token).__name__` 判断是否 AI 回复：`AIMessage` / `AIMessageChunk` 是模型输出的累积片段。
- `content_blocks`：新版本 ChatQwen 返回的"内容块"结构，取 `type == 'text'` 的块里的 `text`。
- 每块 yield 一个 `{"type": "content", "data": ...}` 字典——这就是 chat_stream 接口收到的 chunk。

**⑧ 会话历史读写**
```python
def get_session_history(self, session_id):
    checkpoint_tuple = self.checkpointer.get(config)   # 取检查点
    checkpoint_data = checkpoint_tuple.checkpoint      # 兼容元组/dict 两种形态
    messages = checkpoint_data.get("channel_values", {}).get("messages", [])
    # 跳过 SystemMessage、跳过带 lc_source=summarization 的摘要消息
    # 转成前端要的 {"role", "content", "timestamp"} 格式
def clear_session(self, session_id):
    self.checkpointer.delete_thread(session_id)   # 删掉整个会话的检查点
```
- 从 LangGraph 检查点反推会话历史。`channel_values` 是图里共享状态的快照。
- 过滤 `lc_source == "summarization"` 的摘要消息：避免前端历史里出现"Here is a summary..."这种**伪装成用户**的伪消息（上下文压缩的副作用），非常细致的处理。

**知识点**：`create_agent`、TypedDict 状态、`add_messages`、MemorySaver/thread_id、流式 astream、上下文压缩中间件、中文 token 计数、懒加载与降级。

---

### 4.7 MCP 客户端

#### 文件：`app/agent/mcp_client.py`

**职责**：管理 MCP 客户端（单例）、加载 MCP 工具、失败重试、异常格式化。

```python
_mcp_client: Optional[MultiServerMCPClient] = None   # 全局单例（延迟初始化）

def format_exception_chain(exc) -> str:
    """展开 ExceptionGroup/TaskGroup，打印真实的子异常链"""
    sub_exceptions = getattr(exc, "exceptions", None)
    if sub_exceptions is not None:
        lines = [str(exc)] + [f"  [{i}] {format_exception_chain(sub)}" for i, sub in enumerate(sub_exceptions)]
        return "\n".join(lines)
    msg = f"{type(exc).__name__}: {exc}"
    cause = exc.__cause__ or exc.__context__   # 显式/隐式异常链
    if cause is not None and cause is not exc:
        return f"{msg}\n  caused by: {format_exception_chain(cause)}"
    return msg
```
- **异常链展开**：Python 3.11 的 `ExceptionGroup`（如 asyncio 任务组）里会包着多个子异常，直接 `str(e)` 看不到细节。这个函数递归展开成可读的树。用来打日志很有效。

```python
async def load_mcp_tools_safe(client) -> tuple[list, str | None]:
    try:
        tools = await client.get_tools()
        return tools, None
    except BaseException as e:
        return [], format_exception_chain(e)   # 失败返回错误文本，不抛

async def retry_interceptor(request, handler, max_retries=3, delay=1.0):
    for attempt in range(max_retries):
        try:
            result = await handler(request)
            return result
        except Exception as e:
            last_error = e
            if attempt < max_retries - 1:
                wait_time = delay * (2 ** attempt)   # 指数退避：1s → 2s → 4s
                await asyncio.sleep(wait_time)
    return CallToolResult(content=[TextContent(type="text", text=error_msg)], isError=True)
```
- **拦截器**：在"工具调用请求 → 真正执行"之间插一层。失败用**指数退避**重试 3 次，全失败返回一个 `isError=True` 的工具结果——这样大模型能看到"工具失败了"，而不是整个对话崩掉。这是"让 LLM 自己处理错误"的典型做法。

```python
async def get_mcp_client(servers=None, tool_interceptors=None, force_new=False):
    global _mcp_client
    if force_new:
        return _create_mcp_client(servers or DEFAULT_MCP_SERVERS, tool_interceptors)
    if _mcp_client is None:                     # 单例：第一次创建，之后复用
        _mcp_client = _create_mcp_client(...)
    return _mcp_client
```
- **单例模式**：整个应用只连一次 MCP，避免每次对话都重连。`force_new` 用于特殊场景（不同配置）。

```python
def suggest_mcp_transport(url, transport) -> str | None:
    if "/sse" in lower_url and transport.replace("_", "-") in ("streamable-http", "http"):
        return "URL 含 /sse/ 但 transport=streamable-http，腾讯云托管端点应使用 sse"
    ...
```
- 一个小"顾问"函数：URL 和 transport 明显不匹配时给提示（只提示不自动改）。因为 MCP 有两种常见形态：腾讯云托管端点用 `/sse/` + `sse`，本地 FastMCP 用 `/mcp` + `streamable-http`。

**知识点**：MCP 客户端、单例、拦截器/重试、指数退避、`BaseException` 捕获（含 asyncio.CancelledError）、异常链格式化。

**联系**：`rag_agent_service`、`planner`、`executor`、`replanner` 都通过 `get_mcp_client_with_retry()` 拿工具。

---

### 4.8 AIOps 智能运维（Plan-Execute-Replan）

> 这是项目的另一大卖点。核心思想：**先规划、再执行、边执行边评估要不要调整**。参考了 LangGraph 官方的 Plan-and-Execute 教程。

#### 文件：`app/agent/aiops/state.py`（状态）

```python
class PlanExecuteState(TypedDict):
    input: str                    # 用户任务
    plan: List[str]               # 计划步骤列表
    past_steps: Annotated[List[tuple], operator.add]   # 已执行历史，operator.add 追加
    response: str                 # 最终报告
    skill_name: str               # 命中的技能名
    skill_context: str            # 技能全文
```
- `Annotated[List[tuple], operator.add]`：**关键语法**——告诉 LangGraph 这个字段是"追加"式更新。Executor 每执行一步返回 `{"past_steps": [(task, result)]}`，LangGraph 自动把它 append 到历史里，而不是覆盖。

#### 文件：`app/agent/aiops/planner.py`（计划制定）

```python
class Plan(BaseModel):
    steps: List[str] = Field(description="完成任务所需的不同步骤...")

planner_prompt = ChatPromptTemplate.from_messages([("system", dedent("""...{tools_description}...{experience_context}...""")), ("placeholder", "{messages}")])

async def planner(state):
    # 步骤0: Skill 优先
    skill = skill_matcher.match(input_text)
    if skill is not None:
        return {"plan": skill.steps, "skill_name": skill.name, "skill_context": skill.full_text}
    # 步骤1: RAG 检索相关经验文档
    context_str = await retrieve_knowledge.ainvoke({"query": input_text})
    # 步骤2: 获取本地工具 + MCP 工具，格式化描述
    # 步骤4: LLM 生成计划
    llm = ChatQwen(model=config.rag_model, api_key=config.dashscope_api_key, temperature=0)
    planner_chain = planner_prompt | llm.with_structured_output(Plan)
    plan_result = await planner_chain.ainvoke({"messages": [...], "tools_description": tools_description, "experience_context": experience_context})
    return {"plan": plan_result.steps}
```

**逐段讲解**：
- `Plan` 是 Pydantic 模型，`with_structured_output(Plan)` 强制模型输出 `{"steps": [...]}` 结构，而不是自由文本。这是"把 LLM 输出变成程序能用的结构化数据"的标准手法。
- `planner_prompt | llm.with_structured_output(Plan)`：`|` 管道运算符——LangChain 的链式组合（prompt → LLM → 结构化输出）。
- **Skill 优先**：先问技能匹配器有没有命中的标准流程。命中就直接用技能的固定步骤，跳过 LLM 规划（可靠、可控）；没命中才走 RAG 检索经验 + LLM 生成计划。这就是 4.9 技能系统与 AIOps 的结合点。
- `retrieve_knowledge.ainvoke({"query": input_text})`：把知识检索工具当成普通函数调用（返回 content 字符串）。
- 异常兜底：规划失败返回一个默认三步计划，保证流程能继续。

#### 文件：`app/agent/aiops/executor.py`（执行步骤）

```python
async def executor(state):
    plan = state.get("plan", [])
    task = plan[0]    # 取第一个步骤
    llm = ChatQwen(...)
    llm_with_tools = llm.bind_tools(all_tools)   # 绑定工具
    tool_node = ToolNode(all_tools)              # 工具执行节点
    # 根据是否 Skill 模式选择 system_content（Skill 模式注入完整技能定义）
    messages = [SystemMessage(content=system_content), HumanMessage(content=f"请执行以下任务: {task}")]
    llm_response = await llm_with_tools.ainvoke(messages)   # 1. LLM 决定是否调工具
    if llm_response.tool_calls:                              # 2. 有工具调用
        messages.append(llm_response)
        tool_messages = await tool_node.ainvoke({"messages": messages})  # 执行工具
        messages.extend(tool_messages["messages"])
        final_response = await llm_with_tools.ainvoke(messages)          # 3. 结果给 LLM 生成总结
        result = final_response.content
    else:
        result = llm_response.content    # 没调工具，直接输出
    return {"plan": plan[1:], "past_steps": [(task, result)]}   # 移除已执行步骤 + 追加历史
```

**逐段讲解**：
- **一次执行一个步骤**（`plan[0]`），执行完返回 `{"plan": plan[1:]}` 把这一步从计划里移除，`past_steps` 用 `operator.add` 追加——这就是图上"循环"的实现方式。
- 手动的 ReAct 循环：LLM 决定调工具 → `ToolNode` 执行 → 把工具结果喂回给 LLM 生成总结。因为 LangGraph 的 `ToolNode` 一次只处理一轮工具调用。
- **Skill 模式**：如果命中了技能，`system_content` 会把整个技能流程定义塞进去，并提示"严格按流程规定的方法和参数执行"。
- 容错：异常时把失败结果也记入 `past_steps`（"执行失败: ..."），流程继续，让 replanner 决定怎么办。

#### 文件：`app/agent/aiops/replanner.py`（评估/重规划/出报告）

```python
class Act(BaseModel):
    action: str        # 'continue' | 'replan' | 'respond'
    new_steps: List[str] = []   # replan 时的新步骤

class Response(BaseModel):
    response: str      # 最终报告

replanner_prompt = ChatPromptTemplate.from_messages([("system", dedent("""...
    你有三个选择（按优先级排序）：
    1. 'respond' - 信息充足，立即生成最终响应 【最高优先级】
    2. 'continue' - 当前计划合理，继续执行 【次优先级】
    3. 'replan' - 当前计划有严重问题 【最低优先级，谨慎使用】
    ...""")), ("placeholder", "{messages}")])
```

**逐段讲解**：
- 这是 AIOps 的"大脑决策层"。用精心设计的提示词告诉模型决策优先级：**"优先结束 > 保持不变 > 调整计划"**，"信息足够就响应，不要追求完美"——防止模型无限执行。
- `MAX_STEPS = 8` 强制上限：执行超过 8 步直接出报告，物理防止死循环。
- `len(new_steps) > len(plan)` 时截断；`len(past_steps) >= 5` 时禁止 replan——**双重硬约束**防止模型乱改计划导致无限循环。
- **Skill 模式禁止 replan**：命中了技能就走固定流程，不许模型改步骤（改了就偏离标准流程了），步骤跑完直接出报告。
- `_generate_response`：把原始任务 + 执行历史整理好，用 `response_prompt | llm.with_structured_output(Response)` 生成 Markdown 报告。失败时降级生成一个"已收集信息"的简单报告。

#### 文件：`app/services/aiops_service.py`（组装成图 + 流式输出）

**① 构建图**
```python
workflow = StateGraph(PlanExecuteState)
workflow.add_node("planner", planner)
workflow.add_node("executor", executor)
workflow.add_node("replanner", replanner)
workflow.set_entry_point("planner")
workflow.add_edge("planner", "executor")
workflow.add_edge("executor", "replanner")

def should_continue(state):
    if state.get("response"):      # 已生成最终报告 → 结束
        return END
    if state.get("plan"):          # 还有步骤 → 继续执行
        return "executor"
    return END                     # 计划跑完且无响应 → 结束

workflow.add_conditional_edges("replanner", should_continue, {"executor": "executor", END: END})
compiled_graph = workflow.compile(checkpointer=self.checkpointer)
```
- `StateGraph`：创建状态图。`add_node` 加节点，`add_edge` 加固定边，`add_conditional_edges` 加**条件边**（根据 `should_continue` 的返回值选走向）。
- 图结构：`planner → executor → replanner → (条件) → executor 或 END`。这就是"执行→评估→决定要不要再执行"的循环。

**② 流式执行**
```python
async for event in self.graph.astream(input=initial_state, config=config_dict, stream_mode="updates"):
    for node_name, node_output in event.items():
        if node_name == "planner": yield self._format_planner_event(node_output)
        elif node_name == "executor": yield self._format_executor_event(node_output)
        elif node_name == "replanner": yield self._format_replanner_event(node_output)
final_state = self.graph.get_state(config_dict)   # 取最终状态
final_response = final_state.values.get("response", "")
yield {"type": "complete", ..., "diagnosis": {...}}
```
- `stream_mode="updates"`：每次一个节点更新就产出一个事件。
- `_format_planner_event` 等：把原始状态翻译成前端友好的事件结构（`plan` 事件、`step_complete` 事件、`report` 事件）。这就是 api/aiops.py 收到的事件。
- 执行完用 `graph.get_state(config)` 拿最终状态里的 `response`，包装成 `complete` 事件。

**③ 诊断入口 `diagnose`**
```python
aiops_task = dedent("""诊断当前系统是否存在告警...输出格式要求：...""")  # 一大段任务模板
alert_hint = _fetch_active_alert_hint()   # 注入当前活跃告警名
if alert_hint: aiops_task += alert_hint
async for event in self.execute(aiops_task, session_id): ...
```
- `_fetch_active_alert_hint`：调用 `query_prometheus_alerts_api` 拿当前活跃告警名，拼进任务文本——让 Planner 能按告警名直接命中对应技能（比如告警叫 `HighCPUUsage`，就能命中 `handle_cpu_high_usage` 技能）。Prometheus 不可用时静默降级（返回空串）。
- `diagnose` 把 `complete` 事件包装成兼容旧接口的 `diagnosis` 格式。

**知识点**：LangGraph StateGraph、条件边、stream_mode="updates"、结构化输出、Skill 集成、硬约束防死循环。

---

### 4.9 Skill 业务技能系统

> 背景：知识库 RAG 是把 SOP 当"参考文本"检索出来，模型**自由决定**怎么用；Skill 是主动匹配，命中后把步骤**固化**为执行计划，**严格执行**。前者灵活但不可控，后者可控但僵化——两者互补。

#### 文件：`app/skills/loader.py`（技能加载器）

```python
_STEP_RE = re.compile(r"^\d+[\.、]\s*(.*)$")   # 匹配 "1. xxx" / "1、xxx"

@dataclass
class Skill:
    name: str
    description: str
    when_to_use: List[str]   # 触发关键词
    steps: List[str]         # 固定步骤
    full_text: str           # SKILL.md 全文
    path: Path

def _parse_frontmatter(text):    # 手写解析 --- 包裹的元数据
    parts = text.split("---", 2)
    for line in meta_text.strip().splitlines():
        key, _, value = line.partition(":")
        meta[key.strip()] = value.strip()
    return meta, body.lstrip("\n")

def _extract_steps(body):        # 只收集 "## 执行步骤" 章节的有序列表
    for line in body.splitlines():
        if stripped.startswith("##"):
            in_section = stripped == "## 执行步骤"
            continue
        if in_section:
            m = _STEP_RE.match(stripped)
            if m: steps.append(m.group(1).strip())

class SkillLoader:
    def load_all(self):          # 懒加载 + 缓存
        if self._cache is None:
            self._cache = self._scan()
        return self._cache
    def _scan(self):             # 扫描 app/skills/*/SKILL.md
        for skill_dir in sorted(self.root.glob("*/")):
            ...
```

**逐段讲解**：
- `SKILL.md` 是技能文件格式（参考 Claude Code 的 skill 格式）：`---` 包 frontmatter（name/description/when_to_use），正文有 `## 执行步骤`（固定步骤）、`## 常见原因分析`、`## 验证步骤` 等章节。
- **刻意不用 pyyaml**：frontmatter 只有三个固定 key，手写解析即可，省一个运行时依赖。这种"按需引入依赖"的判断值得学习。
- `_extract_steps` 只认 `## 执行步骤` 章节里的 `1. xxx` 行，`## 验证步骤` 等其他章节的列表不会被误收集。
- `load_all` 用缓存（首次扫描，之后复用）；`refresh` 清缓存（新增技能后调用）。

#### 文件：`app/skills/matcher.py`（技能匹配器）

```python
class SkillMatcher:
    def match(self, text) -> Optional[Skill]:
        lowered = text.lower()
        best, best_score = None, 0
        for skill in skill_loader.load_all():
            score = sum(1 for kw in skill.when_to_use if kw and kw.lower() in lowered)  # 子串匹配计数
            if score > best_score:
                best, best_score = skill, score
        return best if best_score > 0 else None   # 得分必须 > 0 才算命中
```

**逐段讲解**：
- **规则匹配**（关键词子串），不用 embedding 语义匹配。注释给了理由：技能数量少、告警名+中文短语用子串匹配足够可靠，且不引入 embedding 的运行时依赖。**技术选型"够用就好"**是重要思维。
- 得分 = 命中的关键词数，取最高；>0 才命中，否则返回 None（走 RAG 兜底）。

#### 文件：`app/skills/cpu_high_usage/SKILL.md`（技能示例）

看 frontmatter 和开头：
```markdown
---
name: handle_cpu_high_usage
description: 处理 CPU 使用率过高告警（HighCPUUsage）的标准诊断与处理流程
when_to_use: HighCPUUsage, CPU使用率, CPU 使用率, cpu 使用率, cpu 高, ...
---

## 执行步骤
1. 使用 `get_current_time` 获取当前时间，作为后续日志查询的时间范围基准
2. 使用 `query_logs` 查询系统日志：地域 `ap-guangzhou`，日志主题 `system-metrics`...
3. 从日志结果中定位 CPU 消耗进程...
...
## 常见原因分析
- **死循环或无限递归**：...
## 验证步骤
...
## 升级机制 / 联系方式
```

**逐段讲解**：这就是"运维专家的流程知识"的结构化：`when_to_use` 负责触发，`## 执行步骤` 负责固定流程（被 loader 解析成 `steps`），`## 常见原因分析` 等章节负责给 Executor 提供判断依据（全文作为 `skill_context` 注入）。`service_unavailable/SKILL.md` 同理。

**知识点**：提示词/流程知识的文件化、DSL 式的轻量协议。

---

### 4.10 MCP 服务器（模拟数据服务）

> 这两个文件是**独立进程**，用 FastMCP 框架写成 MCP Server，通过 HTTP 提供工具。主服务通过 `mcp_client` 连接它们（transport=streamable-http）。它们现在返回**模拟数据**（mock），生产要接真实 API（README 有说明）。

#### 文件：`mcp_servers/cls_server.py`（日志查询服务，端口 8003）

```python
mcp = FastMCP("CLS")

def log_tool_call(func):   # 装饰器：每个工具调用都打日志（方法名/参数/结果摘要）
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        logger.info(f"调用方法: {method_name}")
        ...
    return wrapper

@mcp.tool()
@log_tool_call
def get_current_timestamp() -> int:
    """获取当前时间戳（毫秒）..."""
    return int(datetime.now().timestamp() * 1000)

@mcp.tool()
@log_tool_call
def get_region_code_by_name(region_name): ...   # 地区映射表（mock）

@mcp.tool()
@log_tool_call
def search_topic_by_service_name(service_name, region_code=None, fuzzy=True): ...  # 服务名→日志主题

@mcp.tool()
@log_tool_call
def search_log(topic_id, start_time, end_time, query=None, limit=100): ...  # 按时间范围查日志（mock 数据）
```

**逐段讲解**：
- `@mcp.tool()`：把函数注册成 MCP 工具，`docstring` 会被暴露给大模型看（所以 docstring 写得特别详细，包括参数格式、示例——**给 LLM 看的工具文档就是"接口契约"**）。
- `@log_tool_call` 装饰器套在 `@mcp.tool()` 外面：工具调用全程记录日志（装饰器嵌套顺序：`@mcp.tool()` 先执行注册，`@log_tool_call` 再包装执行逻辑）。
- `search_log` 用 `topic_id` 判断，动态生成模拟日志（每 1 分钟一条 INFO）。这就是 mock 的实现方式。
- 底部 `if __name__ == "__main__": mcp.run(transport="streamable-http", host="127.0.0.1", port=8003, path="/mcp")`：以 streamable-http 模式起服务。**主服务 config.py 里 `mcp_cls_url = http://localhost:8003/mcp` 与这里的 host/port/path 严格对应。**

#### 文件：`mcp_servers/monitor_server.py`（监控数据服务，端口 8004）

- 工具：`query_cpu_metrics`、`query_memory_metrics`。都接收 `service_name` + 时间范围 + `interval`，返回随时间变化的数据点 + 统计（avg/max/min/p95）+ 告警提示。
- 数据生成算法（比如 CPU）：前 3 个点 10% 左右，之后指数增长冲到 96%——模拟"故障逐步恶化"的曲线，配合阈值检测（>80% 触发告警提示）。
- 底部同理起在 8004 端口 `/mcp`。

**逐段讲解**：这两个文件主要是让 AIOps 的 Executor 有"工具可调"。它们演示了 **MCP Server 怎么把一个业务能力（查日志/查监控）包装成标准工具**。学习重点不是 mock 算法，而是：`@mcp.tool()` + 详细 docstring + transport/端口配置的对应关系。

**知识点**：FastMCP、工具注册、mock 数据、装饰器、docstring 即契约。

---

### 4.11 前端（简述）

`static/` 是纯静态单页应用：

- `index.html`：页面骨架。有聊天区、输入框，还有一个"智能运维与诊断工具"入口。
- `app.js`：前端逻辑。关键点：
  - 普通对话用 `fetch('/api/chat')` 一次性拿结果。
  - 流式对话用 `fetch('/api/chat_stream', {method: 'POST', ...})` + 读 `response.body` 的流，或 `EventSource`，解析 SSE 的 `data` 字段里的 `{type, data}`，按 `type` 渲染。
  - AIOps 诊断用 `EventSource('/api/aiops')` 或 fetch stream，渲染 `plan`/`step_complete`/`report`/`complete` 事件。
- `styles.css`：样式。

> 前端不需要深入。你只需要理解：**前端唯一的工作就是把后端吐的 SSE 事件翻译成界面**。

---

## 第 5 章 三大核心机制深挖

### 5.1 RAG 全链路（从上传到回答）

**一次完整的 RAG 问答，数据是这样流动的：**

```
【写】上传文档
  file.py → vector_index_service → parser → splitter → Milvus(biz表)

【读】用户提问
  retrieve_knowledge 工具
    ├─ query 向量化（text-embedding-v4, 1024维）
    ├─ Milvus 检索（IVF_FLAT + L2）→ 召回 20 条（RAG_RECALL_K）
    ├─ gte-rerank-v2 重排 → 按相关性排序
    ├─ 取前 3 条（RAG_TOP_K）
    └─ 格式化成「参考资料 N」上下文 → 塞给大模型
```

**为什么重排？** 向量检索只看"语义相似"，可能把相关但没用的段落排前面；重排模型专门做"query 与段落的相关性"精细打分。召回 20 → 重排 → 取 3，兼顾"查得全"和"排得准"。

### 5.2 Plan-Execute-Replan（诊断循环）

```
用户任务（或固定诊断模板 + 活跃告警）
   ↓
planner：技能命中？→ 用技能步骤。否则 RAG 检索经验 + LLM 生成步骤
   ↓
executor：执行 plan[0]，LLM 决定调哪些工具（查日志/监控/时间），ToolNode 执行，结果记入 past_steps，plan 移除该步
   ↓
replanner：LLM 决策 continue / replan / respond（带硬约束防死循环）
   ↑________继续循环________↓
   respond → 生成 Markdown 诊断报告
```

**防死循环的护栏（多层）：**
1. 提示词强约束："信息足够就 respond，不要追求完美"。
2. `replanner` 代码硬限制：新步骤 ≤ 剩余步骤；已执行 ≥ 5 禁止 replan；已执行 ≥ 8 强制 respond。
3. Skill 模式直接禁止 replan。

### 5.3 Skill 技能与 RAG 的互补

| | RAG（知识库） | Skill（技能） |
|---|---|---|
| 触发方式 | 语义检索 | 关键词规则匹配 |
| 执行 | LLM 自由发挥 | 步骤固化，严格执行 |
| 优点 | 灵活、能答各种问题 | 可控、符合专家流程 |
| 缺点 | 不可控，可能偏离流程 | 只覆盖写好的场景 |
| 在 AIOps 里 | Planner 检索经验制定计划 | 命中后直接采用固定步骤 |

### 5.4 上下文自动压缩（SummarizationMiddleware）

- 问题：多轮对话的上下文会无限增长，最终超过模型的输入窗口（`qwen-max` 最大 30720 tokens）。
- 方案：上下文占用达到 70% 时，把**早期消息总结成一段摘要**，替换掉原文，只保留最近 20 条完整消息。
- 关键细节：**中文 token 计数必须用真实 tokenizer**（`get_num_tokens`），否则按字符/4 估算会低估 4 倍，压缩永远不触发。
- 副作用处理：压缩产生的摘要消息带 `lc_source=summarization` 标记，前端历史展示时会过滤掉。

---

## 第 6 章 动手练习

学代码最有效的方式是"改它、看它变"。以下练习按难度递增，**先备份要改的文件**。

**练习 1：改配置看效果**
改 `.env` 里 `RAG_TOP_K=3` 为 `RAG_TOP_K=1`，重启服务，问同一个知识库问题，观察回答的引用变少。感受"上下文里文档数量"的影响。

**练习 2：给系统提示词加人设**
改 `rag_agent_service.py` 的 `_build_system_prompt`，加一句"你是一个说话简短、爱用表情的助手"，重启问它问题。感受提示词对模型行为的直接影响。

**练习 3：新增一个本地工具**
模仿 `time_tool.py`，在 `app/tools/` 里新建 `calc_tool.py`，用 `@tool` 写一个 `add(a, b) -> str` 的加法工具，然后在 `tools/__init__.py` 的 `DEFAULT_LOCAL_AGENT_TOOLS` 里加上它。问"23 加 45 等于几"，看 Agent 会不会调工具。这是理解"大模型工具调用"最好的实验。

**练习 4：观察流式事件**
用浏览器打开页面，F12 打开 Network，看 `/api/chat_stream` 的响应体里 `data:` 开头的行——你能看到 `{"type":"tool_call",...}` 和一堆 `{"type":"content",...}` 交替出现。直观理解 SSE。

**练习 5：加一个 Skill**
模仿 `cpu_high_usage/SKILL.md`，新建 `app/skills/disk_full/SKILL.md`（frontmatter 写 `when_to_use: 磁盘, 磁盘满了, disk full`，`## 执行步骤` 写 3-4 步）。重启后对 AIOps 说"磁盘满了"，看 Planner 是否直接采用你的步骤（日志里会打"命中业务技能"）。

**练习 6：改 MCP 服务器数据**
把 `cls_server.py` 的 `search_log` 里 mock 日志内容从"正在同步元数据……"改成你的话，重启 cls_server，让 AIOps 重新诊断，看诊断报告引用的日志变化。理解"工具数据从哪里来"。

**练习 7：读懂一次诊断的完整链路**
对着 `logs/app_*.log`，触发一次 `/api/aiops`，按时间顺序跟踪日志：skill 匹配 → planner → executor（工具调用）→ replanner → 报告。把日志和代码对应起来。

---

## 第 7 章 常见问题排查

| 症状 | 原因与处理 |
|------|-----------|
| `ConnectionNotExistException: should create connection first` | 访问 Collection 前没连 Milvus。项目已用猴子补丁 + 提前 connect 规避。确认 Docker 里 Milvus 起来了：`docker ps \| grep milvus` |
| `DASHSCOPE_API_KEY` 相关报错 / 401 | `.env` 没配密钥或格式错。确认键名与 `config.py` 字段一致，重启服务 |
| 启动就崩，提示 Milvus 相关 | `vector_store_manager` 在 import 时就连接 Milvus。先把 Docker 起来再启动服务 |
| 中文回答乱码 | 日志乱码检查 `logger.py` 的 `encoding="utf-8"`；接口乱码检查 `json.dumps(..., ensure_ascii=False)` |
| MCP 工具加载失败 | 先单独起 `mcp_servers/cls_server.py`（8003）和 `monitor_server.py`（8004），再起主服务。检查 `config.py` 的 url 与服务器端口一致 |
| 问了知识库问题但回答没引用 | 确认文档已上传且索引成功（看日志 "文件索引完成"）；确认 `.env` 的 `RAG_TOP_K > 0` |
| 上下文被压缩成摘要 | 这是正常现象（`SummarizationMiddleware`），配置在 `rag_context_*` 三项 |

---

## 附录：名词速查表

| 名词 | 一句话解释 |
|------|-----------|
| LLM / ChatModel | 大语言模型。本项目用阿里千问 qwen-max |
| RAG | 检索增强生成：先从知识库检索相关段落，再让模型基于它回答 |
| Embedding | 把文本变成向量。语义相近的向量距离近 |
| Vector DB（Milvus） | 存向量、按相似度检索的数据库 |
| Retriever | 检索器，根据 query 从向量库召回文档 |
| Rerank | 重排，对召回的候选再精排一次 |
| Agent（智能体） | 能用工具的 LLM 程序：决定调哪个工具、解读结果、最终作答 |
| Tool | 工具，LLM 可以调用的函数 |
| LangGraph | 用图（节点+边+状态）编排 LLM 流程的框架 |
| StateGraph | LangGraph 的状态图 |
| checkpointer | 检查点，保存图运行状态，用于会话管理/断点续跑 |
| thread_id | 会话 ID，LangGraph 靠它区分不同会话 |
| MemorySaver | 把检查点存在内存里的实现 |
| MCP | 模型上下文协议，给 AI 接工具的行业标准 |
| FastMCP | 快速写 MCP Server 的框架 |
| SSE | 服务端向客户端单向推流的技术，用于流式输出 |
| Plan-Execute-Replan | 计划→执行→评估（继续/调整/出结果）的循环模式 |
| Skill | 结构化的标准业务流程文件（SKILL.md） |
| SummarizationMiddleware | LangChain 的上下文自动压缩中间件 |
| DashScope | 阿里云百炼，本项目大模型/嵌入/重排模型的提供方 |

---

*祝你学习愉快。读代码最好的心态是"把它拆开看看里面有什么"——现在你有了地图，动手吧。*

# 更新日志：对话上下文自动压缩（SummarizationMiddleware）

> 功能：当大模型上下文窗口占用达到 70% 时，自动把早期对话压缩成摘要，保证长对话不超限、早期关键信息不丢失。
> 日期：2026-08-09

---

## 一、背景：现在的对话是怎么存的

先回答一个基础问题：**多轮对话为什么需要"上下文"？**

大模型（比如 qwen-max）没有记忆。每次回答都只靠"你这次发给它的全部文字"。所以要把历史对话也一起发过去，模型才能"记得"你之前说了什么。这些一起发给模型的历史对话，就叫**上下文**。

项目里的实现（`app/services/rag_agent_service.py`）：

```
FastAPI 收到问题
   → 拼上之前的全部对话（存在内存 MemorySaver 里）
   → 一次性发给 qwen-max
   → 模型回答 → 把新问题+新回答再存回内存
```

问题在哪？**内存里的消息只进不出，无限累积**。对话越来越长，总有一天超过模型一次能接收的上限（上下文窗口），程序就会报错。

之前代码里其实有一个 `trim_messages_middleware`（按条数删消息），但它**定义了却从来没接入**——是一段死代码。这次把它删掉了，换成真正有用的方案。

---

## 二、调研结论：LangChain 自带能力，直接复用

你要求"先看看 LangChain 框架有没有自动压缩总结的功能"。

**结论：有。** LangChain 1.3.14 内置了 `SummarizationMiddleware`（中文可理解为"摘要中间件"），`from langchain.agents.middleware import SummarizationMiddleware` 就能用。

它挂在 `create_agent(middleware=[...])` 上，作用在每次调用模型**之前**（所以叫 `before_model`）：

```
每次要发消息给模型之前，先检查一下：
  当前上下文占用 ≥ 70% 了吗？
    ↓ 是
  把"较早的对话"丢给模型总结成一段摘要
  → 用摘要替换掉旧对话，只保留最近 N 条原文
  → 再把（更短的）内容发给模型
```

关键点：它是**压缩成摘要**，不是简单丢弃。早期对话的重要信息被"浓缩"保留下来，模型依然"知道"早期聊了什么。

---

## 三、设计决策（为什么这么配）

| 决策点 | 选择 | 原因 |
|--------|------|------|
| 70% 的基数 | 按 **30720**（qwen-max 最大输入 tokens） | qwen-max 上下文总长 32768 = 输入 30720 + 输出 8192。触发线 = 30720 × 70% = **21504 tokens** |
| 触发方式 | `trigger=("fraction", 0.7)` | 按"上下文占用比例"触发，比"按条数"更科学——不同模型窗口不同，比例自动适配 |
| 压缩后保留 | `keep=("messages", 20)` | 保留最近 20 条消息原文，之前的都进摘要 |
| 死代码 `trim_messages_middleware` | **删除** | 从未接入，留着误导人；且它只是"按条数删"，不如摘要方案 |
| 配置化 | 进 `config.py` + `.env` | 和重排功能一致，改参数不用改代码 |

### 三个需要理解的技术细节

**1. 为什么要声明 `profile={"max_input_tokens": ...}`？**

`fraction`（比例）触发模式需要知道"模型上下文多大"才能算出 70% 是多少。LangChain 从模型对象的 `profile.max_input_tokens` 读这个数字。**不声明会直接报错**（ValueError），不会静默跳过。所以给 `ChatQwen` 加了这个 profile：

```python
ChatQwen(..., profile={"max_input_tokens": config.rag_context_window})
```

**2. 为什么必须自定义 token 计数器？（中文的坑）**

这是本次最容易踩的坑。判断"上下文占用 70%"要先统计当前对话占了多少 token。`SummarizationMiddleware` 默认用一个"估算"算法 `count_tokens_approximately`——按**字符数除以 4** 粗略估计。

对英文大概准，但对**中文严重低估**。实测：295 个中文字符，估算只有 **73** tokens，实际是 **275** tokens——低估了近 4 倍！

后果很危险：模型上下文已经快满了，估算却显示"才 20%"→ 永远不触发压缩 → 程序报"超出上下文限制"错误。

所以代码里自定义了 `_count_tokens`，改用 `self.model.get_num_tokens`（底层是 tiktoken 分词器，**和模型真正使用的分词方式一致，中文精确**）：

```python
def _count_tokens(self, messages) -> int:
    total = 0
    for msg in messages:
        content = getattr(msg, "content", None)
        # 处理多块内容（如工具返回的 [{type:text,...}, ...]）
        if isinstance(content, list):
            parts = [b.get("text", "") if isinstance(b, dict) else str(b) for b in content]
            text = "".join(parts)
        else:
            text = content if isinstance(content, str) else str(content)
        total += self.model.get_num_tokens(text)
    return total
```

**3. 触发逻辑到底怎么算的？（实测确认，避免误解）**

看框架源码 `_should_summarize` 后实测确认：

- **触发只看 token 比例**：`总 tokens ≥ 阈值(21504)` 就触发，**不要求消息条数**。上下文占 70% 了，哪怕只有 10 条消息也该压缩。
- `keep=20` 不是触发条件，而是"压缩后保留最近 20 条"：25 条消息时 cutoff=5（前 5 条进摘要，后 20 条原文保留）；消息数 ≤20 时不压缩（没有可压缩的）。

---

## 四、改动清单

### 1. `app/config.py` —— 新增 3 个配置字段

```python
# RAG 上下文自动压缩配置
rag_context_window: int = 30720        # 模型最大输入 tokens（qwen-max max_input，不含输出）
rag_context_trigger_fraction: float = 0.7  # 上下文占用达到该比例时触发摘要压缩
rag_context_keep_messages: int = 20    # 压缩后保留的最近消息条数
```

### 2. `.env` —— 新增 3 个配置项（未改动任何密钥值）

```bash
# RAG 上下文自动压缩配置
# 上下文占用达到该比例时，自动把早期对话总结成摘要（qwen-max max_input=30720）
RAG_CONTEXT_WINDOW=30720
RAG_CONTEXT_TRIGGER_FRACTION=0.7
RAG_CONTEXT_KEEP_MESSAGES=20
```

### 3. `app/services/rag_agent_service.py` —— 核心改动（4 处）

**① 删掉死代码 `trim_messages_middleware`（原来约 37 行）**

同时清理了只给它用的 import：删掉 `RemoveMessage`、`REMOVE_ALL_MESSAGES`。保留 `add_messages` 和 `Annotated`（`AgentState` 还在用）。

**② 给模型声明上下文大小（配合比例触发）**

```python
self.model = ChatQwen(
    model=self.model_name,
    api_key=config.dashscope_api_key,
    temperature=0.7,
    streaming=streaming,
    profile={"max_input_tokens": config.rag_context_window},  # 新增
)
```

**③ 创建摘要中间件 + 自定义精确 token 计数器**

```python
# 上下文自动压缩中间件：
# 当上下文占用达到 rag_context_trigger_fraction（默认 70%）时，
# 自动把早期对话总结成摘要替换，保留最近 rag_context_keep_messages 条消息
self.summarization = SummarizationMiddleware(
    self.model,
    trigger=("fraction", config.rag_context_trigger_fraction),
    keep=("messages", config.rag_context_keep_messages),
    token_counter=self._count_tokens,  # 自定义，避免中文计数低估
)
```

**④ 挂载到 Agent 上**

```python
self.agent = create_agent(
    self.model,
    tools=all_tools,
    checkpointer=self.checkpointer,
    middleware=[self.summarization],  # 新增
)
```

**⑤ `get_session_history` 过滤摘要消息（防前端显示伪消息）**

压缩发生时，框架往历史里插入一条摘要消息，结构是：

```python
HumanMessage(
    content="Here is a summary of the conversation to date:\n\n【摘要内容】",
    additional_kwargs={"lc_source": "summarization"},  # ← 标记
)
```

如果不处理，前端会话历史里会出现一条"用户消息"，内容是英文的 "Here is a summary..."——很怪异。所以在 `get_session_history` 里跳过带这个标记的消息：

```python
# 跳过上下文压缩生成的摘要消息（带 lc_source=summarization 标记），
# 避免前端历史出现 "Here is a summary..." 的伪用户消息
if msg.additional_kwargs.get("lc_source") == "summarization":
    continue
```

**⑥ 顺带修复：`get_session_history` 读取 checkpoint 的方式**

真实运行时发现：`checkpointer.get()` 返回的是 `Checkpoint`（dict 类型，含 `channel_values`），而原代码假设返回的是命名元组/普通元组，用 `checkpoint_tuple[0]` 取数——真实环境下会抛 `KeyError: 0` 被 `except` 吞掉，导致**前端会话历史一直为空**。修复为兼容三种形式：

```python
if hasattr(checkpoint_tuple, 'checkpoint'):
    checkpoint_data = checkpoint_tuple.checkpoint      # 命名元组
elif isinstance(checkpoint_tuple, dict):
    checkpoint_data = checkpoint_tuple                # Checkpoint dict
else:
    checkpoint_data = checkpoint_tuple[0] if checkpoint_tuple else {}  # 普通元组
```

### 4. `更新日志-上下文压缩.md` —— 本文档

---

## 五、验证结果

### 语法 / 配置 / mock 验证（全部通过，19/19）

| 验证项 | 结果 |
|--------|------|
| `py_compile` 语法检查（`config.py` + `rag_agent_service.py`） | ✅ 通过 |
| 配置加载：window=30720 / fraction=0.7 / keep=20 | ✅ 正确 |
| middleware 挂载成功，trigger=`[{'fraction': 0.7}]`，keep=`('messages', 20)` | ✅ |
| 模型 profile `max_input_tokens=30720` 已声明 | ✅ |
| 自定义 token_counter 生效 | ✅ |
| **中文计数精确**：295 字符 → 275 tokens（估算只有 73，验证不低估） | ✅ |
| 阈值计算 = 30720×0.7 = **21504** | ✅ |
| A: 20 条 + 超阈值 → 触发 | ✅ |
| B: 20 条 + 未超阈值 → 不触发 | ✅ |
| C: 10 条 + 超阈值 → 触发（确认：触发只看 token 比例） | ✅ |
| D: keep=20 → 25 条消息 cutoff=5（保留最近 20 条） | ✅ |
| E: keep=20 → 10 条消息 cutoff=0（无需压缩） | ✅ |
| 摘要消息结构：HumanMessage + `lc_source=summarization` | ✅ 实测确认 |
| `get_session_history` 过滤摘要消息（历史 5 条 → 返回 4 条，role 顺序正确） | ✅ |
| `get_session_history` 普通历史不受影响 | ✅ |

### 真实端到端触发验证（已执行，低成本法）

用环境变量把 `RAG_CONTEXT_TRIGGER_FRACTION` 临时降到 **0.01**（仅当前进程生效，未改 `.env`），真实调用 qwen-max 连续多轮短对话：

| 验证项 | 结果 |
|--------|------|
| 触发时机 | ✅ 第 8 轮（消息数 21）触发压缩 |
| 摘要消息生成 | ✅ `additional_kwargs={'lc_source': 'summarization'}` |
| 摘要内容 | ✅ qwen-max 生成结构化摘要（SESSION INTENT / SUMMARY / NEXT STEPS） |
| 压缩后保留最近 20 条 | ✅ 前端历史 14 条真实对话，role 全 user/assistant 交替 |
| 前端历史无 "Here is a summary..." 伪消息 | ✅ |
| `get_session_history` 正常返回历史 | ✅（顺带修复了取数 bug，见上文 ⑥） |
| MCP 连接失败影响 | ✅ 仅后台警告，不影响压缩与回答 |

验证脚本已按约定删除。正常运行时把 `RAG_CONTEXT_TRIGGER_FRACTION` 恢复为 `0.7`（本次用的是进程内环境变量覆盖，`.env` 未改动）。

---

## 六、如何自己验证端到端

前置：Milvus 已启动、`.env` 已配好 key。

**方法一（推荐，低成本）：调低触发门槛临时看效果**

临时把 `.env` 的 `RAG_CONTEXT_TRIGGER_FRACTION` 调到很小（比如 `0.01`），重启服务。随便聊几轮，观察服务日志会出现摘要压缩行为（早期消息被替换成摘要）；前端会话历史**不会**出现 "Here is a summary..." 条目。验证完恢复 `0.7`。

**方法二：正常 70% 真实触发**

需要把当前会话的上下文累积到 21504 tokens 以上（约 2 万 tokens 的中文长对话），之后提问观察。成本较高，不推荐日常验证。

---

## 七、注意事项与限制

- **压缩是有损的**：早期细节进摘要后不再是逐字原文。`keep=20` 就是权衡——最近 20 条保原文，更早的进摘要。想要保留更多原文，调大 `RAG_CONTEXT_KEEP_MESSAGES`。
- **触发线 = `RAG_CONTEXT_WINDOW × RAG_CONTEXT_TRIGGER_FRACTION`**。换更大的模型窗口时，记得同步改 `.env` 里的 `RAG_CONTEXT_WINDOW`。
- **`_count_tokens` 依赖模型的真实分词器**，实测精确。但如果未来换成不支持的模型（`get_num_tokens` 异常），压缩判定会报错——这是"宁可报错也不静默低估"的设计，与重排功能"不降级"同理。
- **摘要消息只影响发给模型的内容**，不影响 MemorySaver 里存的消息；`get_session_history` 的过滤只影响前端展示。
- 若想彻底关闭压缩：把 `RAG_CONTEXT_TRIGGER_FRACTION` 调到大于 1（比如 `99.9`），实际不可能触发。

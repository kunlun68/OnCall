# 更新日志：上传文档自动向量化支持多文档类型（md / txt / pdf / docx）

> 功能：上传知识库文档自动向量化从"只支持 .txt/.md"扩展为支持 **md、txt、pdf、docx** 四种类型。系统按上传文件的扩展名自动匹配对应的文本提取器，提取出纯文本后再走统一的分片流程。
> 日期：2026-08-09

---

## 一、背景：为什么之前只有 txt / md

知识库的向量化链路是这样的：

```
上传文件
  → 提取文件内容（变成一段字符串）
  → 把长文本切成小块（分片，chunking）
  → 每一块生成向量 → 存入 Milvus
```

之前代码里"提取文件内容"这一步写死了 `path.read_text(encoding="utf-8")`——**把文件当纯文本直接读**。

这个写法对 txt、md 没问题，但对二进制格式会直接崩：

- **pdf / word（.docx）本质是二进制**（pdf 是带内容流的对象集合，docx 是个 zip 包），按 UTF-8 读会抛 `UnicodeDecodeError`，索引直接失败。
- 而且"所有文件一律按纯文本读"没有扩展余地——以后想加 Excel、HTML 都得改这段逻辑。

所以这次的核心工作是：**把"提取文件内容"这步抽象出来，按文件类型分派到不同的处理器**。

---

## 二、总体设计：两条原则

### 1. 提取层（parser）与分片层（splitter）职责分离

改动前，读取和分片耦合在一个函数里。现在拆成两层，各管一件事：

| 层 | 模块 | 职责 | 输入 → 输出 |
|----|------|------|------------|
| **文本提取层** | `document_parser_service`（新建） | 把"各种格式的文件"变成**一段字符串** | 文件 → 纯文本 |
| **分片层** | `document_splitter_service`（原有） | 把"一段字符串"切成适合向量化的**块** | 纯文本 → 分片列表 |

为什么分离？因为两者变化的理由不同：

- **提取层**随"文件格式种类"变化（加一种格式 = 加一个处理器）。
- **分片层**随"知识库使用场景"变化（想切细一点、按标题切，都只改分片器）。

拆开后，pdf 和 docx 提取出的**纯文本**和 txt 没有区别，天然走同一条分片逻辑，分片器完全不用感知新格式。

### 2. "支持哪些格式"只有一份名单

支持格式的名单 `SUPPORTED_EXTENSIONS` 定义在 parser 服务里，**三处引用方都从它 import**：

- 上传路由 `file.py`（白名单校验，决定能否上传）
- 目录索引 `vector_index_service.py`（遍历目录时过滤文件）
- parser 自身（分派表）

之前 `file.py` 和 `vector_index_service` 各自维护一份 `["txt","md"]` 名单，漏改一处就会出现"上传接口认了、索引却跳过"的割裂。统一后加新格式只改一个地方。

---

## 三、四种类型怎么提取文本

### 1. txt / md：直接读 UTF-8

最简单，文件本身就是文本，一个 `read_text(encoding="utf-8")` 搞定。所以一个 `_extract_plain` 处理器同时服务两种扩展名。

### 2. pdf：pypdf 逐页提取（为什么逐页）

**PDF 的内部结构**：一个 PDF 是一批"对象"的集合，其中每页对应一个**内容流（content stream）**——里面记录了这页画了什么文字、图片、矢量。文字以"字体+坐标+字符"的形式存在，不是普通文本行。

`pypdf` 做的事情是：解析每一页的内容流，按文本绘制命令还原出文字，`page.extract_text()` 返回这一页的文字。

**为什么要逐页循环**而不是整个文件一把抓？

```python
for page in reader.pages:
    text = page.extract_text()
```

因为 PDF 没有"全文"这个概念，文字是按页存放的。pypdf 也提供了 `reader.pages[i].extract_text()` 这种按页接口，没有"一次取全书"的接口。逐页提取后，页与页之间用 `"\n"` 连接，还原阅读顺序。

### 3. docx：python-docx 遍历段落（为什么遍历段落）

**docx 的内部结构**：一个 docx 文件本质是 **zip 压缩包**，解开后里面是多个 XML 文件，正文在 `word/document.xml`。文档正文是一段一段（paragraph）存的，每段有样式、字体等格式信息。

`python-docx` 做的：解包 → 解析 XML → 把文档暴露成 `doc.paragraphs` 列表。每个 `paragraph.text` 就是这一段去掉格式后的纯文本。

```python
doc = Document(file_path)
paragraphs = [p.text for p in doc.paragraphs if p.text and p.text.strip()]
content = "\n".join(paragraphs)
```

两个设计点：

- **跳过空段落**（`if p.text and p.text.strip()`）：文档里大量回车产生的空段落，提取出来只会产生 `\n\n` 空行，污染后续分片。
- **表格本期不提取**：`doc.paragraphs` 只覆盖正文段落，不含 `doc.tables` 里的内容。如果文档内容主要在表格里，提取会为空。这是有意的简化，扩展点（遍历 `doc.tables`）已写在注释里。

### 4. 为什么 Word 只支持 .docx，老 .doc 拒绝

`.doc` 是老版 Word 格式（Office 2003 及以前）。两者内部结构完全不同：

- `.doc` 是**二进制格式**，没有公开的纯 Python 解析标准库。
- `.docx` 是 **Office Open XML**（zip + XML），规范公开，`python-docx` 就是为它写的。

`.doc` 改名为 `.docx` 没用——改名只变扩展名，内部还是旧格式，python-docx 打不开。所以上传路由对 `.doc` 单独提示：**"不支持老版 .doc 格式，请在 Word 中另存为 .docx 后重新上传"**。

---

## 四、每种类型的具体分片方法

提取出纯文本后，进入 `document_splitter_service.split_document`。**只判断是不是 markdown**，其余类型统一走普通文本分片：

| 类型 | 文本提取 | 分片方法 |
|------|----------|----------|
| `.md` | read_text 直读 | **两阶段分片**：① `MarkdownHeaderTextSplitter` 按标题切 → ② `RecursiveCharacterTextSplitter` 二次切 → ③ 合并过小的碎片 |
| `.txt` | read_text 直读 | `RecursiveCharacterTextSplitter`（chunk_size=1600, overlap=100） |
| `.pdf` | pypdf 逐页提取 | 提取为纯文本后 → `RecursiveCharacterTextSplitter`（同上） |
| `.docx` | python-docx 遍历段落 | 提取为纯文本后 → `RecursiveCharacterTextSplitter`（同上） |

**关键：md 走标题分片，其它走普通分片，为什么？**

因为 md 有**结构化信息（标题）**。知识库检索时，用户搜"部署步骤"应该命中带 `## 部署步骤` 标题的那个块。按标题分片能保证标题和它的内容留在同一块里，检索更精准。

而 pdf / docx 提取出的纯文本**已经丢失了结构**（图片、表格、排版都没了），只剩一段连续文字，没有标题可依，所以统一走 `RecursiveCharacterTextSplitter`。

### 分片参数（为什么 1600/100）

看 `document_splitter_service.py` 的初始化：

```python
self.text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=self.chunk_size * 2,   # 1600 = 800 × 2
    chunk_overlap=self.chunk_overlap, # 100
)
```

- **chunk_size=1600**：每块最多约 1600 字符。这是 `config.chunk_max_size=800` 的**两倍**——注释写明了原因："加倍 chunk_size，减少分片数"。块大 → 分片少 → 向量少 → 检索时命中更完整。
- **overlap=100**：相邻两块重叠 100 字符。作用：一句话如果恰好横跨两块边界，两头都保留一部分，检索时不会因为截断而丢语义。

### markdown 的三阶段

```
原文（含 # 和 ## 标题）
  ↓ 阶段① MarkdownHeaderTextSplitter
  按一级/二级标题切成若干大段（strip_headers=False 保留标题文字在内容里）
  ↓ 阶段② RecursiveCharacterTextSplitter
  大段再按 1600 字符切成若干块
  ↓ 阶段③ 合并小于 300 字符的碎片
  相邻的小碎片粘到前一/后一块上，避免出现"半句话"孤零零一块
```

为什么要有阶段③？md 按标题切完，经常出现一个标题下只有一行字的情况——单独成一块太碎，检索价值低，还浪费向量额度。`_merge_small_chunks` 把它并入相邻块（合并后不超过 1600 才并）。

---

## 五、扩展名分派表（dispatcher）

parser 的核心是这张分派表：

```python
_dispatcher: Dict[str, str] = {
    ".md": "_extract_plain",
    ".txt": "_extract_plain",
    ".pdf": "_extract_pdf",
    ".docx": "_extract_docx",
}
```

`extract_text` 的流程：

1. 取扩展名（`Path(file_path).suffix.lower()`，**统一转小写**，兼容 `.MD`）。
2. 在表里查扩展名 → 拿到处理器**方法名**。
3. 查不到 → `ValueError("不支持的文件类型: xxx")`，由上层转成 400 错误。
4. 查到 → `getattr(self, handler_name)` 拿到方法再调用。

**为什么存方法名字符串而不是直接存方法对象？**

最初直接写 `{".md": _extract_plain}`（方法对象）会抛 `NameError`——类体的方法定义在类创建时才真正建立，而 `_dispatcher` 写在方法定义之前，此时名字还不存在。存字符串 + `getattr` 运行时解析，绕开了这个顺序问题。**新增一种格式只需两步**：写一个 `_extract_xxx` 方法 + 在表里登记一行。

---

## 六、边界情况（防御性处理）

| 情况 | 处理 | 结果 |
|------|------|------|
| 空文档 / 全空白 | 返回 `""`，打 warning 日志 | 后续分片层判空返回空列表，不会崩 |
| **加密 PDF** | `reader.is_encrypted` → 先 `decrypt("")` 试空密码；仍加密则抛 `RuntimeError("PDF 已加密，无法解析…")` | 很多"已加密"文件其实只是无密码限制，空密码能解开；真加密明确报错而不是乱读 |
| **损坏文件**（如 .pdf 内容不是 pdf） | pypdf 抛异常 → 统一转成 `RuntimeError` | 不静默，日志记录原因，上层记入 failed_files |
| **扫描件 / 图片型 PDF** | 没有文本层，`extract_text()` 返回空 → 返回 `""` + warning | 已知限制：需要 OCR 才能读，本期不做 |
| 空 docx（无段落） | 返回 `""` + warning | 同上 |
| `.doc` 老格式 | 上传路由特判，400 + 提示"另存为 .docx" | 比"不支持的文件格式"更可操作 |
| 大写扩展名 `.PDF` | `suffix.lower()` 统一小写 | 顺带修复了原来 `.MD` 漏检 |

所有 `_extract_*` 内部统一模式：`except Exception → logger.error(...) → raise RuntimeError(f"解析失败 {path}: {e}")`。**只对空内容静默**，其余异常全部向上抛并记录，符合项目"失败要看得见"的风格。

---

## 七、逐文件改动一览

### 1. 新建 `app/services/document_parser_service.py`（核心）

- `SUPPORTED_EXTENSIONS = frozenset({".md", ".txt", ".pdf", ".docx"})`——格式名单唯一来源。
- `DocumentParserService`——提取层，`extract_text(file_path) -> str` 是唯一对外入口。
- `_extract_plain` / `_extract_pdf` / `_extract_docx`——三个处理器。
- 模块级单例 `document_parser_service`，与项目其他 service 的惯例一致。

### 2. `app/services/vector_index_service.py`

两处改动：

- **L159**：`content = path.read_text(encoding="utf-8")` → `content = document_parser_service.extract_text(str(path))`。核心接通点，从此读文件全部走提取层。
- **L96-99**：目录索引的 glob 从"硬编码后缀"改为 `p.suffix.lower() in SUPPORTED_EXTENSIONS`，以 parser 名单为唯一来源。

### 3. `app/services/document_splitter_service.py`

只改一行：`split_document` 里判断 markdown 从 `file_path.endswith(".md")` 改为 `Path(file_path).suffix.lower() == ".md"`。原因：旧写法对 `.MD` 大小写不敏感场景会漏判；且这里只关心"是不是 md"，让其余类型自然落入普通分片分支。

### 4. `app/api/file.py`

- 删除本地 `ALLOWED_EXTENSIONS` 名单，改为 `from document_parser_service import SUPPORTED_EXTENSIONS`。
- 校验从 `file_extension not in ALLOWED_EXTENSIONS` 改为 `f".{file_extension}" not in SUPPORTED_EXTENSIONS`（统一带点小写）。
- 错误提示用 `SUPPORTED_EXTENSION_NAMES` 动态生成（"仅支持: docx, md, pdf, txt"），加格式自动变。
- 特判 `.doc` 给出"另存为 .docx"的可操作提示。

### 5. `pyproject.toml`

新增两个依赖（注释注明用途）：

```toml
"pypdf>=4.0.0",      # 多文档类型支持：PDF 文本解析
"python-docx>=1.1.0", # 多文档类型支持：Word(.docx) 文本解析
```

**为什么不用 langchain-community 的 PDF/Word loader？** langchain-community 已进入 sunset（停止维护）阶段，官方建议直接用底层库。pypdf 本身也是 LangChain 默认使用的 PDF 解析后端，python-docx 是 Word 解析的事实标准——直接用独立库，去掉了 loader 这层薄封装。

---

## 八、验证结果

- 单元验证 `extract_text`：txt/md/docx/pdf 四类提取正常；空段落跳过、空白 PDF 返回 `""`、加密 PDF 抛 `RuntimeError`、损坏文件抛 `RuntimeError`、不支持扩展名抛 `ValueError` —— 全过。
- 分片验证：4 种类型各生成分片，metadata 的 `_extension` 字段分别正确。
- **端到端**：4 个真实文件（txt/md/pdf/docx）一次 `index_directory` 全部索引成功（4/4 成功），在真实 Milvus `biz` collection 中按 `metadata["_source"]` 逐一命中确认，4 种类型齐全，测试数据已清理。

---

## 九、对分片/检索的影响（需要知道的后续知识）

1. **PDF/docx 提取会丢结构**：排版、表格、图片都拿不到。所以这两类文档**标题语义**在分片时发挥不出来（markdown 标题分片只对 md 生效）。如果知识库 PDF 主要是带标题的规范文档，后续可考虑 `PyMuPDF` 做"按标题层级"的提取。
2. **扫描件 PDF 读不出字**：需要 OCR（如 PaddleOCR）预处理，本期明确不做。
3. **`_extension` 元数据已写入 Milvus**：以后可以按文件类型过滤检索（例如只查 PDF 来源的文档），metadata 字段已备好。

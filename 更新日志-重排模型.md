# 更新日志：接入百炼重排模型（Rerank）

> 功能：RAG 知识检索接入阿里云百炼重排模型 `gte-rerank-v2`，对向量检索召回结果做二次精排。
> 日期：2026-08-09

---

## 一、背景与动机

原检索链路是**单阶段召回**：

```
用户问题 → Embedding 向量化 → Milvus 向量检索 Top-3 → 直接作为上下文给 LLM
```

问题在于：向量相似度（L2 距离）衡量的是**文本层面的语义相近**，但它和"对当前问题是否真正有用"并非完全等价。经常出现：与 query 向量最接近的 3 条里混入无关内容，真正相关的内容被挤掉。

业界标准的解法是**两阶段检索**：

```
用户问题 → Embedding 向量化 → Milvus 召回 Top-K（K 更大，如 20）
        → 重排模型精排 → 取前 Top-3 → 作为上下文给 LLM
```

第一阶段用向量检索"宽召回"保证不漏；第二阶段用**交叉编码器（Cross-Encoder）重排模型**做"精排"——它把 query 和每条文档拼在一起过一遍模型，能捕捉更细粒度的相关性，排序质量远高于纯向量距离。

本次改动就是引入第二阶段。

---

## 二、设计决策（为什么这么改）

### 1. 重排模型：`gte-rerank-v2`（阿里云百炼）
- 多语言排序模型，RAG 场景主流选择，价格低（0.8 元/百万 tokens）。
- 可通过 `.env` 的 `DASHSCOPE_RERANK_MODEL` 切换（如 `qwen3-rerank`），无需改代码。

### 2. 「不降级」语义
你明确要求**不降级**。含义：**重排失败时必须报错，绝不静默回退到「仅向量检索」的未重排结果**——因为那样会把"重排"这个环节当成摆设，用户得到的还是旧质量的答案，且不会发现重排已经坏了。

实现上有三层保障：
- 重排服务只抛**专用异常 `RerankError`**（见后文），绝不返回"部分成功"的结果。
- `dashscope` SDK 有个坑：**HTTP 调用失败时不抛异常**，只返回 `status_code != 200` 的响应对象。若代码只检查 `resp.output.results` 而不检查状态码，失败时会**静默得到空结果**——这正是最危险的"隐性降级"。所以代码里**必须显式校验 `resp.status_code == 200`**。
- `retrieve_knowledge` 工具里 `except RerankError` 捕获后直接 `raise`（重新抛出），让错误明确传给上层：RAG 对话由 LangChain 框架回馈给 LLM（用户能看到报错），AIOps 诊断由 Planner 记录 warning。

### 3. 召回-精排：新增 `RAG_RECALL_K`
- 原 `RAG_TOP_K=3` 保留，语义变为"**重排后最终返回给 LLM 的条数**"。
- 新增 `RAG_RECALL_K=20`，语义为"**重排前从向量库召回的候选条数**"。
- 完整链路：`召回 20 条 → 重排 → 取前 3 条`。

### 4. 为什么不加 `RAG_RERANK_ENABLED` 开关
开关本身就是一个"可静默降级"的配置面，与"不降级"冲突。若真要临时关掉重排，把 `RAG_RECALL_K` 调到等于 `RAG_TOP_K` 即可让重排退化为透传——这是一个**显式、可审查**的动作，不是静默的。

### 5. api_key 为什么必须显式传
项目配置走 `pydantic-settings`（从 `.env` 读进 `config` 对象），**并不会把变量写入系统环境变量**。而 `dashscope` SDK 默认从环境变量 `DASHSCOPE_API_KEY` 读 key。所以调用 `TextReRank.call()` 时必须显式传 `api_key=config.dashscope_api_key`，否则会取到空 key 导致认证失败。

### 6. 单条候选跳过重排
召回结果 ≤1 条时直接返回，不调用重排 API。这是**优化**（省一次调用），不是降级——单条结果没有"排序"需求。

---

## 三、改动清单

### 1. `app/config.py` —— 新增 2 个配置字段

```python
# DashScope 配置区新增：
dashscope_rerank_model: str = "gte-rerank-v2"  # 百炼重排模型

# RAG 配置区新增，RAG_TOP_K 注释同步更新：
rag_recall_k: int = 20  # 重排前从向量库召回的候选文档数
```

- `rag_top_k` 的注释从"返回数量"改为"重排后返回给 LLM 的最终文档数"，明确职责分工。

### 2. `.env` —— 新增 2 个配置项（未改动任何密钥值）

```bash
# 重排模型
DASHSCOPE_RERANK_MODEL=gte-rerank-v2
# 重排前召回候选数
RAG_RECALL_K=20
```

### 3. `app/services/vector_rerank_service.py` —— 新建（核心）

新增 `RerankService`，职责：**把"调用百炼重排"封装成一个不降级的服务**。

关键结构：

```python
class RerankError(Exception):
    """重排失败专用异常，与检索失败区分"""

class VectorRerankService:
    def rerank(self, query, documents, top_n) -> List[Tuple[Document, float]]:
        # 1. 空/单条直接返回（优化，非降级）
        # 2. 防御：超过 MAX_RERANK_DOCS(100) 截断
        # 3. 调 TextReRank.call(..., api_key=self.api_key)  ← 显式传 key
        # 4. 校验 resp.status_code == 200，否则抛 RerankError  ← 不降级唯一强制点
        # 5. 按 result.index 映射回原始 Document（results 已按分降序）
        # 6. 越界索引 / 空结果 → 抛 RerankError
```

单例导出：`vector_rerank_service`，风格与项目其他 service 一致。

### 4. `app/tools/knowledge_tool.py` —— 接入重排（一处改动，RAG + AIOps 同时生效）

```python
# 召回 k 从 rag_top_k(3) 改为 rag_recall_k(20)
retriever = vector_store.as_retriever(search_kwargs={"k": config.rag_recall_k})

# 新增：重排精排，取 rag_top_k 条
ranked = vector_rerank_service.rerank(query, docs, config.rag_top_k)
docs = [doc for doc, _ in ranked]

# 新增：不降级——重排异常捕获后直接 re-raise（放在 except Exception 之前）
except RerankError as e:
    logger.error(f"知识重排失败: {e}")
    raise
```

为什么改这一处就够了？因为 `retrieve_knowledge` 是**唯一实际使用的知识检索入口**，被两条链路共用：
- **RAG 对话**：`rag_agent_service.py` 的工具集 `DEFAULT_LOCAL_AGENT_TOOLS` 包含它；
- **AIOps 诊断**：`planner.py` 第 83 行 `await retrieve_knowledge.ainvoke(...)` 用它取经验文档。

所以"改一处，全局生效"，符合你"全部生效"的决策。

### 5. `更新日志-重排模型.md` —— 本文档

---

## 四、验证结果

### 单元 / mock 验证（全部通过）

| 验证项 | 结果 |
|--------|------|
| `py_compile` 语法检查（3 个改动文件） | ✅ 通过 |
| 配置加载（`rag_recall_k=20` / `rag_top_k=3` / `dashscope_rerank_model=gte-rerank-v2`） | ✅ 正确 |
| 空候选重排 → 返回 `[]`（不调用 API） | ✅ |
| 单条候选重排 → 返回 `[(doc, 1.0)]`（不调用 API） | ✅ |
| 成功路径 index 映射（index=2 → 第 3 个 doc，score=0.95） | ✅ |
| **不降级**：HTTP 400 → 抛 `RerankError` | ✅ |
| **不降级**：SDK 内部异常 → 抛 `RerankError` | ✅ |
| **不降级**：重排空结果 → 抛 `RerankError` | ✅ |
| `retrieve_knowledge` 召回 k=20 → 重排返回 3 条 → `format_docs` 正常 | ✅ |
| `retrieve_knowledge` 空召回 → 返回"没有找到相关信息。" | ✅ |
| `retrieve_knowledge` 重排失败 → **re-raise `RerankError`（不降级）** | ✅ |

### 真实端到端验证（已执行）

环境：启动 Milvus（Docker Compose，`milvusdb/milvus:v2.5.10`），`biz` 集合共 **21 条**文档，真实调用百炼 API。

| 验证项 | 结果 |
|--------|------|
| 查询 `CPU使用率高如何排查` 触发 `retrieve_knowledge` | ✅ |
| 召回候选数 | ✅ `候选文档数: 20` |
| 重排生效 | ✅ 百炼返回 `request_id=a5f8cf0c-4900-9670-b913-588471359c38` |
| 重排分数降序 | ✅ `index=2 (0.3694)` → `index=0 (0.3429)` → `index=14 (0.2954)` |
| 返回 3 条且内容正确 | ✅ CPU 排查步骤、CPU 告警处理、内存告警（cpu_high_usage.md / memory_high_usage.md） |
| 耗时 | 3.75s（embedding + 向量检索 + 重排） |
| **真实不降级**：改用非法模型 `gte-rerank-v9` 真实调用 | ✅ 百炼返回 `400 InvalidParameter "Model not exist."` → 抛出 `RerankError`，未返回未重排结果 |

---

## 五、如何再次验证端到端

前置：Milvus 已启动（`docker compose -f vector-database.yml up -d`）、`.env` 已配好 `DASHSCOPE_API_KEY`。

1. 启动 FastAPI 服务后提问 RAG 问题，观察日志：
   - 应看到 `调用重排模型 gte-rerank-v2, 候选文档数: 20` 和 `重排完成, request_id=..., 返回 3 个文档`。
2. **验证重排生效**：日志里的 `score` 是重排模型的相关性分（0~1，越高越相关），可对比返回的 3 条与纯向量距离排序的差异。
3. **验证不降级**：临时把 `.env` 的 `DASHSCOPE_RERANK_MODEL` 改成不存在的模型名（如 `gte-rerank-v9`），重启后提问，应看到日志记录 `重排服务返回异常状态: 400`，且 RAG 回答出现报错而非旧质量的未重排内容。验证完恢复。

---

## 六、注意事项与限制

- **gte-rerank-v2 限制**：单次请求文档数上限约 200（官方建议 100 以内），单文档 30000 token 内。本项目 `RAG_RECALL_K=20` 远低于限制，无需担心；代码里仍加了 `MAX_RERANK_DOCS=100` 防御截断。
- **成本**：重排按 token 计费（文本输入 0.8 元/百万 tokens）。每次 RAG 检索把 20 条候选（每条 ≤800 字符的分块）发给重排模型，单次成本极低。
- **AIOps 的容错**：AIOps Planner 查询经验文档失败时会记录 warning 并继续（这是 Planner 的可选提示设计，不是重排降级）。
- 若未来要换重排模型，只需改 `.env` 的 `DASHSCOPE_RERANK_MODEL`，无需动代码。
